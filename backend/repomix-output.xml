This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
middleware/
  authMiddleware.js
models/
  user.js
prisma/
  migrations/
    20260505064945_init_users/
      migration.sql
    20260510203257_add_products/
      migration.sql
    20260510203826_add_products/
      migration.sql
    20260604185305_add_refresh_tokens/
      migration.sql
    20260605114426_add_reviews_and_orders/
      migration.sql
    20260609135153_add_password_reset_fields/
      migration.sql
    20260612193035_add_password_reset_cloudinary/
      migration.sql
    20260630072956_add_subcategory/
      migration.sql
    20260701122503_add_new_fields/
      migration.sql
    20260703093122_add_recipe_table/
      migration.sql
    20260705120642_add_cart_item/
      migration.sql
    20260712192811_add_address_middlename_sms/
      migration.sql
    migration_lock.toml
  schema.prisma
  seed.js
routes/
  cart.js
scripts/
  linkNewProductPhotos.js
  migrateImagesToCloudinary.js
.gitignore
check-users.js
Dockerfile
make-admin.js
package.json
prisma.config.js
script.js
server.js
test-db.js
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="middleware/authMiddleware.js">
const jwt = require("jsonwebtoken");

// ==========================================
// MIDDLEWARE АВТОРИЗАЦІЇ
// ==========================================
// Перевіряє JWT токен у кожному захищеному запиті.
// Якщо токен є і він правильний — пропускає далі.
// Якщо немає або невірний — повертає 401.

const authMiddleware = (req, res, next) => {
  // Токен приходить у заголовку: Authorization: Bearer <token>
  const authHeader = req.headers.authorization;

  // Якщо заголовка взагалі немає
  if (!authHeader) {
    return res.status(401).json({ error: "Токен відсутній" });
  }

  // Витягуємо сам токен (після слова "Bearer ")
  const token = authHeader.split(" ")[1];

  if (!token) {
    return res.status(401).json({ error: "Токен відсутній" });
  }

  try {
    // Перевіряємо чи токен справжній
    const decoded = jwt.verify(token, process.env.JWT_SECRET);

    // Зберігаємо дані юзера в запит — щоб роути могли їх використати
    req.user = decoded; // містить { userId, email, isAdmin }

    next(); // пропускаємо далі
  } catch (error) {
    return res.status(401).json({ error: "Невірний або прострочений токен" });
  }
};

module.exports = authMiddleware;
</file>

<file path="models/user.js">

</file>

<file path="prisma/migrations/20260505064945_init_users/migration.sql">
-- CreateTable
CREATE TABLE "User" (
    "id" SERIAL NOT NULL,
    "email" TEXT NOT NULL,
    "password" TEXT NOT NULL,
    "firstName" TEXT,
    "lastName" TEXT,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,

    CONSTRAINT "User_pkey" PRIMARY KEY ("id")
);

-- CreateIndex
CREATE UNIQUE INDEX "User_email_key" ON "User"("email");
</file>

<file path="prisma/migrations/20260510203257_add_products/migration.sql">
-- CreateTable
CREATE TABLE "Product" (
    "id" SERIAL NOT NULL,
    "name" TEXT NOT NULL,
    "price" DOUBLE PRECISION NOT NULL,
    "description" TEXT,
    "image" TEXT,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,

    CONSTRAINT "Product_pkey" PRIMARY KEY ("id")
);
</file>

<file path="prisma/migrations/20260510203826_add_products/migration.sql">
/*
  Warnings:

  - Added the required column `category` to the `Product` table without a default value. This is not possible if the table is not empty.

*/
-- AlterTable
ALTER TABLE "Product" ADD COLUMN     "category" TEXT NOT NULL,
ADD COLUMN     "rating" DOUBLE PRECISION NOT NULL DEFAULT 0;
</file>

<file path="prisma/migrations/20260604185305_add_refresh_tokens/migration.sql">
-- AlterTable
ALTER TABLE "User" ADD COLUMN     "birthDate" TIMESTAMP(3),
ADD COLUMN     "gender" TEXT,
ADD COLUMN     "isAdmin" BOOLEAN NOT NULL DEFAULT false,
ADD COLUMN     "phone" TEXT;

-- CreateTable
CREATE TABLE "RefreshToken" (
    "id" SERIAL NOT NULL,
    "token" TEXT NOT NULL,
    "userId" INTEGER NOT NULL,
    "expiresAt" TIMESTAMP(3) NOT NULL,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,

    CONSTRAINT "RefreshToken_pkey" PRIMARY KEY ("id")
);

-- CreateIndex
CREATE UNIQUE INDEX "RefreshToken_token_key" ON "RefreshToken"("token");

-- AddForeignKey
ALTER TABLE "RefreshToken" ADD CONSTRAINT "RefreshToken_userId_fkey" FOREIGN KEY ("userId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE;
</file>

<file path="prisma/migrations/20260605114426_add_reviews_and_orders/migration.sql">
-- CreateTable
CREATE TABLE "Review" (
    "id" SERIAL NOT NULL,
    "rating" INTEGER NOT NULL,
    "comment" TEXT,
    "userId" INTEGER NOT NULL,
    "productId" INTEGER NOT NULL,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,

    CONSTRAINT "Review_pkey" PRIMARY KEY ("id")
);

-- CreateTable
CREATE TABLE "Order" (
    "id" SERIAL NOT NULL,
    "userId" INTEGER NOT NULL,
    "total" DOUBLE PRECISION NOT NULL,
    "address" TEXT NOT NULL,
    "status" TEXT NOT NULL DEFAULT 'pending',
    "paymentMethod" TEXT NOT NULL DEFAULT 'cash',
    "comment" TEXT,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,

    CONSTRAINT "Order_pkey" PRIMARY KEY ("id")
);

-- CreateTable
CREATE TABLE "OrderItem" (
    "id" SERIAL NOT NULL,
    "orderId" INTEGER NOT NULL,
    "productId" INTEGER NOT NULL,
    "name" TEXT NOT NULL,
    "price" DOUBLE PRECISION NOT NULL,
    "quantity" INTEGER NOT NULL,

    CONSTRAINT "OrderItem_pkey" PRIMARY KEY ("id")
);

-- AddForeignKey
ALTER TABLE "Review" ADD CONSTRAINT "Review_userId_fkey" FOREIGN KEY ("userId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE;

-- AddForeignKey
ALTER TABLE "Review" ADD CONSTRAINT "Review_productId_fkey" FOREIGN KEY ("productId") REFERENCES "Product"("id") ON DELETE CASCADE ON UPDATE CASCADE;

-- AddForeignKey
ALTER TABLE "Order" ADD CONSTRAINT "Order_userId_fkey" FOREIGN KEY ("userId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE;

-- AddForeignKey
ALTER TABLE "OrderItem" ADD CONSTRAINT "OrderItem_orderId_fkey" FOREIGN KEY ("orderId") REFERENCES "Order"("id") ON DELETE CASCADE ON UPDATE CASCADE;
</file>

<file path="prisma/migrations/20260609135153_add_password_reset_fields/migration.sql">
/*
  Warnings:

  - A unique constraint covering the columns `[resetToken]` on the table `User` will be added. If there are existing duplicate values, this will fail.

*/
-- AlterTable
ALTER TABLE "User" ADD COLUMN     "resetToken" TEXT,
ADD COLUMN     "resetTokenExpires" TIMESTAMP(3);

-- CreateIndex
CREATE UNIQUE INDEX "User_resetToken_key" ON "User"("resetToken");
</file>

<file path="prisma/migrations/20260612193035_add_password_reset_cloudinary/migration.sql">
/*
  Warnings:

  - You are about to drop the column `resetToken` on the `User` table. All the data in the column will be lost.
  - You are about to drop the column `resetTokenExpires` on the `User` table. All the data in the column will be lost.

*/
-- DropIndex
DROP INDEX "User_resetToken_key";

-- AlterTable
ALTER TABLE "User" DROP COLUMN "resetToken",
DROP COLUMN "resetTokenExpires";

-- CreateTable
CREATE TABLE "PasswordResetToken" (
    "id" SERIAL NOT NULL,
    "token" TEXT NOT NULL,
    "userId" INTEGER NOT NULL,
    "expiresAt" TIMESTAMP(3) NOT NULL,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,

    CONSTRAINT "PasswordResetToken_pkey" PRIMARY KEY ("id")
);

-- CreateIndex
CREATE UNIQUE INDEX "PasswordResetToken_token_key" ON "PasswordResetToken"("token");

-- AddForeignKey
ALTER TABLE "PasswordResetToken" ADD CONSTRAINT "PasswordResetToken_userId_fkey" FOREIGN KEY ("userId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE;
</file>

<file path="prisma/migrations/20260630072956_add_subcategory/migration.sql">
-- AlterTable
ALTER TABLE "Product" ADD COLUMN     "subcategory" TEXT;
</file>

<file path="prisma/migrations/20260701122503_add_new_fields/migration.sql">
-- Ця міграція описує зміни, які вже фактично застосовані до бази даних
-- (ProductImage, Recipe, нові поля Product), але файл міграції не потрапив у git.
-- Створено заднім числом для узгодження історії міграцій без втрати даних.

-- AlterTable
ALTER TABLE "Product" ADD COLUMN "isPromo" BOOLEAN NOT NULL DEFAULT false,
ADD COLUMN "oldPrice" DOUBLE PRECISION,
ADD COLUMN "subcategory" TEXT;

-- CreateTable
CREATE TABLE "ProductImage" (
    "id" SERIAL NOT NULL,
    "url" TEXT NOT NULL,
    "order" INTEGER NOT NULL DEFAULT 0,
    "productId" INTEGER NOT NULL,

    CONSTRAINT "ProductImage_pkey" PRIMARY KEY ("id")
);

-- CreateTable
CREATE TABLE "Recipe" (
    "id" SERIAL NOT NULL,
    "title" TEXT NOT NULL,
    "image" TEXT,
    "description" TEXT,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,

    CONSTRAINT "Recipe_pkey" PRIMARY KEY ("id")
);

-- AddForeignKey
ALTER TABLE "ProductImage" ADD CONSTRAINT "ProductImage_productId_fkey" FOREIGN KEY ("productId") REFERENCES "Product"("id") ON DELETE CASCADE ON UPDATE CASCADE;
</file>

<file path="prisma/migrations/20260703093122_add_recipe_table/migration.sql">
-- Already applied via 20260701122503_add_new_fields
</file>

<file path="prisma/migrations/20260705120642_add_cart_item/migration.sql">
-- CreateTable
CREATE TABLE "CartItem" (
    "id" SERIAL NOT NULL,
    "userId" INTEGER NOT NULL,
    "productId" INTEGER NOT NULL,
    "quantity" INTEGER NOT NULL DEFAULT 1,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,

    CONSTRAINT "CartItem_pkey" PRIMARY KEY ("id")
);

-- CreateIndex
CREATE UNIQUE INDEX "CartItem_userId_productId_key" ON "CartItem"("userId", "productId");

-- AddForeignKey
ALTER TABLE "CartItem" ADD CONSTRAINT "CartItem_userId_fkey" FOREIGN KEY ("userId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE;

-- AddForeignKey
ALTER TABLE "CartItem" ADD CONSTRAINT "CartItem_productId_fkey" FOREIGN KEY ("productId") REFERENCES "Product"("id") ON DELETE CASCADE ON UPDATE CASCADE;
</file>

<file path="prisma/migrations/20260712192811_add_address_middlename_sms/migration.sql">
-- AlterTable
ALTER TABLE "User" ADD COLUMN     "address" TEXT,
ADD COLUMN     "middleName" TEXT;

-- CreateTable
CREATE TABLE "SmsVerificationCode" (
    "id" SERIAL NOT NULL,
    "phone" TEXT NOT NULL,
    "code" TEXT NOT NULL,
    "expiresAt" TIMESTAMP(3) NOT NULL,
    "used" BOOLEAN NOT NULL DEFAULT false,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,

    CONSTRAINT "SmsVerificationCode_pkey" PRIMARY KEY ("id")
);

-- CreateIndex
CREATE INDEX "SmsVerificationCode_phone_idx" ON "SmsVerificationCode"("phone");
</file>

<file path="prisma/migrations/migration_lock.toml">
# Please do not edit this file manually
# It should be added in your version-control system (e.g., Git)
provider = "postgresql"
</file>

<file path="prisma/schema.prisma">
generator client {
  provider = "prisma-client-js"
  output   = "../node_modules/.prisma/client"
}

datasource db {
  provider = "postgresql"
}

model Product {
  id          Int            @id @default(autoincrement())
  name        String
  description String?
  price       Float
  category    String
  subcategory String?
  image       String?
  images      ProductImage[]
  rating      Float          @default(0)
  isPromo     Boolean        @default(false)
  oldPrice    Float?
  createdAt   DateTime       @default(now())
  reviews     Review[]
  cartItems   CartItem[]
}

model ProductImage {
  id        Int     @id @default(autoincrement())
  url       String
  order     Int     @default(0)  // порядок показу фото
  productId Int
  product   Product @relation(fields: [productId], references: [id], onDelete: Cascade)
}

model User {
  id            Int            @id @default(autoincrement())
  email         String         @unique
  password      String
  firstName     String?
  lastName      String?
  middleName    String?
  phone         String?
  birthDate     DateTime?
  gender        String?
  address       String?
  isAdmin       Boolean        @default(false)
  createdAt     DateTime       @default(now())
  refreshTokens RefreshToken[]
  reviews       Review[]
  orders        Order[]
  passwordResetTokens PasswordResetToken[]
  cartItems     CartItem[]


}

model RefreshToken {
  id        Int      @id @default(autoincrement())
  token     String   @unique
  userId    Int
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  expiresAt DateTime
  createdAt DateTime @default(now())
}

model Review {
  id        Int      @id @default(autoincrement())
  rating    Int      // 1-5
  comment   String?
  userId    Int
  productId Int
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  product   Product  @relation(fields: [productId], references: [id], onDelete: Cascade)
  createdAt DateTime @default(now())
}

model Order {
  id          Int         @id @default(autoincrement())
  userId      Int
  user        User        @relation(fields: [userId], references: [id], onDelete: Cascade)
  total       Float
  address     String
  status      String      @default("pending")
  paymentMethod String    @default("cash")
  comment     String?
  createdAt   DateTime    @default(now())
  items       OrderItem[]
}

model OrderItem {
  id        Int     @id @default(autoincrement())
  orderId   Int
  order     Order   @relation(fields: [orderId], references: [id], onDelete: Cascade)
  productId Int
  name      String
  price     Float
  quantity  Int
}

model PasswordResetToken {
  id        Int      @id @default(autoincrement())
  token     String   @unique
  userId    Int
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  expiresAt DateTime
  createdAt DateTime @default(now())
}

model CartItem {
  id        Int      @id @default(autoincrement())
  userId    Int
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  productId Int
  product   Product  @relation(fields: [productId], references: [id], onDelete: Cascade)
  quantity  Int      @default(1)
  createdAt DateTime @default(now())

  @@unique([userId, productId])
}

model Recipe {
  id          Int      @id @default(autoincrement())
  title       String
  image       String?
  description String?
  createdAt   DateTime @default(now())
}

model SmsVerificationCode {
  id        Int      @id @default(autoincrement())
  phone     String
  code      String
  expiresAt DateTime
  used      Boolean  @default(false)
  createdAt DateTime @default(now())

  @@index([phone])
}
</file>

<file path="prisma/seed.js">
require("dotenv").config();
const { PrismaClient } = require("@prisma/client");
const { PrismaPg } = require("@prisma/adapter-pg");
const { Pool } = require("pg");

const pool = new Pool({ connectionString: process.env.DATABASE_URL });
const adapter = new PrismaPg(pool);
const prisma = new PrismaClient({ adapter });

const products = [
  // ===================== М'ЯСО =====================
  {
    name: "Філе куряче охолоджене",
    price: 89.9,
    category: "М'ясо",
    subcategory: "М'ясо птиці",
    image: "/images/figma/products/chicken.png",
    description: "Свіже куряче філе без кісток.",
    rating: 4.5,
    isPromo: true,
    oldPrice: 109.9,
  },
  {
    name: "Стейк яловичий охолоджений",
    price: 189.9,
    category: "М'ясо",
    subcategory: "Стейки",
    image: "",
    description: "Соковитий яловичий стейк.",
    rating: 4.3,
  },
  {
    name: "Свинина, шийка охолоджена",
    price: 139.9,
    category: "М'ясо",
    subcategory: "Свинина",
    image: "",
    description: "М'яка свиняча шийка для запікання.",
    rating: 4.1,
  },
  {
    name: "Фарш курячий охолоджений",
    price: 74.9,
    category: "М'ясо",
    subcategory: "Фарш",
    image: "",
    description: "Свіжий курячий фарш.",
    rating: 4.0,
  },
  {
    name: "Крила курячі охолоджені",
    price: 64.9,
    category: "М'ясо",
    subcategory: "М'ясо птиці",
    image: "",
    description: "Соковиті курячі крила.",
    rating: 4.2,
  },
  {
    name: "Яловичина, вирізка охолоджена",
    price: 219.9,
    category: "М'ясо",
    subcategory: "Яловичина та телятина",
    image: "",
    description: "Ніжна яловича вирізка.",
    rating: 4.6,
  },
  {
    name: "Стегно куряче охолоджене",
    price: 79.9,
    category: "М'ясо",
    subcategory: "М'ясо птиці",
    image: "",
    description: "Соковите куряче стегно.",
    rating: 4.3,
  },
  {
    name: "Свинина, лопатка охолоджена",
    price: 119.9,
    category: "М'ясо",
    subcategory: "Свинина",
    image: "",
    description: "Свиняча лопатка для гуляшу.",
    rating: 4.0,
  },
  {
    name: "Фарш свинячо-яловичий",
    price: 99.9,
    category: "М'ясо",
    subcategory: "Фарш",
    image: "",
    description: "Змішаний фарш для котлет.",
    rating: 4.2,
  },
  {
    name: "Телятина, гомілка охолоджена",
    price: 249.9,
    category: "М'ясо",
    subcategory: "Яловичина та телятина",
    image: "",
    description: "Нежирна телятина.",
    rating: 4.4,
  },
  {
    name: "М'ясо шашлику зі свинини, мариноване",
    price: 149.9,
    category: "М'ясо",
    subcategory: "М'ясо для шашлику",
    image: "",
    description: "Готове до смаження м'ясо для шашлику.",
    rating: 4.5,
  },
  {
    name: "М'ясо шашлику з курки, мариноване",
    price: 109.9,
    category: "М'ясо",
    subcategory: "М'ясо для шашлику",
    image: "",
    description: "Курятина в маринаді для шашлику.",
    rating: 4.3,
  },
  {
    name: "Перепілка тушка охолоджена",
    price: 159.9,
    category: "М'ясо",
    subcategory: "Дичина",
    image: "",
    description: "Дрібна дичина, перепілка.",
    rating: 4.1,
  },
  {
    name: "Качка тушка охолоджена",
    price: 189.9,
    category: "М'ясо",
    subcategory: "Дичина",
    image: "",
    description: "Свіжа качка для запікання.",
    rating: 4.2,
  },
  {
    name: "Кролик тушка охолоджена",
    price: 229.9,
    category: "М'ясо",
    subcategory: "Кролятина",
    image: "",
    description: "Дієтична крольчатина.",
    rating: 4.3,
  },
  {
    name: "Кролик, стегенця охолоджені",
    price: 199.9,
    category: "М'ясо",
    subcategory: "Кролятина",
    image: "",
    description: "Кролячі стегенця.",
    rating: 4.1,
  },
  {
    name: "Баранина, реберця охолоджені",
    price: 279.9,
    category: "М'ясо",
    subcategory: "Баранина та ягнятина",
    image: "",
    description: "Соковиті баранячі реберця.",
    rating: 4.4,
  },
  {
    name: "Ягнятина, лопатка охолоджена",
    price: 299.9,
    category: "М'ясо",
    subcategory: "Баранина та ягнятина",
    image: "",
    description: "Молода ягнятина.",
    rating: 4.5,
  },
  {
    name: "Сало солоне з часником",
    price: 89.9,
    category: "М'ясо",
    subcategory: "Сало",
    image: "",
    description: "Класичне солоне сало.",
    rating: 4.6,
  },
  {
    name: "Сало копчене",
    price: 99.9,
    category: "М'ясо",
    subcategory: "Сало",
    image: "",
    description: "Копчене сало з прошарком.",
    rating: 4.5,
  },
  {
    name: "Печінка куряча охолоджена",
    price: 54.9,
    category: "М'ясо",
    subcategory: "Субпродукти",
    image: "",
    description: "Свіжа куряча печінка.",
    rating: 4.0,
  },
  {
    name: "Серце куряче охолоджене",
    price: 49.9,
    category: "М'ясо",
    subcategory: "Субпродукти",
    image: "",
    description: "Куряче серце для гуляшу.",
    rating: 3.9,
  },
  {
    name: "Котлети куряча по-київськи, напівфабрикат",
    price: 119.9,
    category: "М'ясо",
    subcategory: "М'ясні напівфабрикати",
    image: "",
    description: "Готова заморожена котлета.",
    rating: 4.3,
  },
  {
    name: "Тефтелі м'ясні, напівфабрикат",
    price: 99.9,
    category: "М'ясо",
    subcategory: "М'ясні напівфабрикати",
    image: "",
    description: "Заморожені тефтелі.",
    rating: 4.1,
  },
  {
    name: "Філе куряче без шкіри, дієтичне",
    price: 84.9,
    category: "М'ясо",
    subcategory: "Дієтичне м'ясо",
    image: "",
    description: "Низькокалорійне куряче філе.",
    rating: 4.4,
  },
  {
    name: "Грудка індички охолоджена",
    price: 149.9,
    category: "М'ясо",
    subcategory: "Дієтичне м'ясо",
    image: "",
    description: "Дієтична грудка індички.",
    rating: 4.5,
  },

  // ===================== СВІЖА РИБА =====================
  {
    name: "Сьомга, стейк охолоджений",
    price: 94.9,
    category: "Свіжа риба",
    subcategory: "Свіжа риба",
    image: "/images/figma/products/fish/fish-1.jpg",
    description: "Свіжий стейк сьомги.",
    rating: 4.3,
  },
  {
    name: "Сьомга (лосось) філе охолоджене",
    price: 109.9,
    category: "Свіжа риба",
    subcategory: "Свіжа риба",
    image: "/images/figma/products/fish/fish-2.jpg",
    description: "Ніжне філе сьомги без кісток.",
    rating: 3.9,
  },
  {
    name: "Форель, стейк охолоджений",
    price: 98.89,
    category: "Свіжа риба",
    subcategory: "Свіжа риба",
    image: "/images/figma/products/fish/fish-3.jpg",
    description: "Стейк форелі преміум якості.",
    rating: 4.3,
  },
  {
    name: "Форель, філе охолоджене",
    price: 109.9,
    category: "Свіжа риба",
    subcategory: "Свіжа риба",
    image: "/images/figma/products/fish/fish-4.jpg",
    description: "Свіже філе форелі.",
    rating: 3.9,
  },
  {
    name: "Голець арктичний охолоджений",
    price: 109.9,
    category: "Свіжа риба",
    subcategory: "Свіжа риба",
    image: "/images/figma/products/fish/fish-5.jpg",
    description: "Арктичний голець.",
    rating: 4.1,
  },
  {
    name: "Форель, філе в упаковці",
    price: 119.9,
    category: "Свіжа риба",
    subcategory: "Заморожена риба",
    image: "/images/figma/products/fish/fish-6.jpg",
    description: "Філе форелі у вакуумній упаковці.",
    rating: 3.4,
  },
  {
    name: "Лосось шотландський, стейки",
    price: 259.9,
    category: "Свіжа риба",
    subcategory: "Свіжа риба",
    image: "/images/figma/products/fish/fish-7.jpg",
    description: "Преміальний лосось.",
    rating: 4.8,
  },
  {
    name: "Лосось, хребти дефростовані",
    price: 16.9,
    category: "Свіжа риба",
    subcategory: "Рибні напівфабрикати",
    image: "/images/figma/products/fish/fish-8.jpg",
    description: "Хребти лосося для бульйонів.",
    rating: 3.4,
  },
  {
    name: "Скумбрія заморожена",
    price: 64.9,
    category: "Свіжа риба",
    subcategory: "Заморожена риба",
    image: "",
    description: "Заморожена скумбрія.",
    rating: 4.2,
  },
  {
    name: "Хек заморожений, тушка",
    price: 54.9,
    category: "Свіжа риба",
    subcategory: "Заморожена риба",
    image: "",
    description: "Біла риба хек.",
    rating: 4.0,
  },
  {
    name: "Креветки очищені заморожені",
    price: 189.9,
    category: "Свіжа риба",
    subcategory: "Морепродукти та молюски",
    image: "",
    description: "Чищені креветки 200 шт/кг.",
    rating: 4.6,
  },
  {
    name: "Мідії в стулках заморожені",
    price: 159.9,
    category: "Свіжа риба",
    subcategory: "Морепродукти та молюски",
    image: "",
    description: "Морські мідії.",
    rating: 4.3,
  },
  {
    name: "Ікра лосося червона",
    price: 459.9,
    category: "Свіжа риба",
    subcategory: "Ікра",
    image: "",
    description: "Делікатесна червона ікра.",
    rating: 4.7,
  },
  {
    name: "Ікра мойви чорна",
    price: 99.9,
    category: "Свіжа риба",
    subcategory: "Ікра",
    image: "",
    description: "Доступна чорна ікра мойви.",
    rating: 4.0,
  },
  {
    name: "Скумбрія копчена",
    price: 89.9,
    category: "Свіжа риба",
    subcategory: "Копчена і в'ялена риба",
    image: "",
    description: "Холодного копчення скумбрія.",
    rating: 4.5,
  },
  {
    name: "Тарань в'ялена",
    price: 119.9,
    category: "Свіжа риба",
    subcategory: "Копчена і в'ялена риба",
    image: "",
    description: "В'ялена тарань до пива.",
    rating: 4.4,
  },
  {
    name: "Палички крабові",
    price: 49.9,
    category: "Свіжа риба",
    subcategory: "Крабові палички",
    image: "",
    description: "Класичні крабові палички.",
    rating: 4.1,
  },
  {
    name: "Філе риби в кляри, напівфабрикат",
    price: 99.9,
    category: "Свіжа риба",
    subcategory: "Рибні напівфабрикати",
    image: "",
    description: "Готова до смаження риба.",
    rating: 4.0,
  },
  {
    name: "Салат з морської капусти",
    price: 64.9,
    category: "Свіжа риба",
    subcategory: "Приготовлена риба, салат",
    image: "",
    description: "Готовий салат з морської капусти.",
    rating: 4.2,
  },
  {
    name: "Лобстер варено-морожений",
    price: 899.9,
    category: "Свіжа риба",
    subcategory: "Устриці та лобстери",
    image: "",
    description: "Преміальний лобстер.",
    rating: 4.8,
  },
  {
    name: "Устриці свіжі, 6 шт",
    price: 459.9,
    category: "Свіжа риба",
    subcategory: "Устриці та лобстери",
    image: "",
    description: "Свіжі французькі устриці.",
    rating: 4.6,
  },
  {
    name: "Равлики Helix заморожені",
    price: 349.9,
    category: "Свіжа риба",
    subcategory: "Заморожені равлики",
    image: "",
    description: "Делікатесні равлики.",
    rating: 4.3,
  },

  // ===================== ФРУКТИ, ОВОЧІ =====================
  {
    name: "Виноград білий без кісточки",
    price: 120.12,
    category: "Фрукти, овочі",
    subcategory: "Фрукти",
    image: "/images/figma/products/grapes.png",
    description: "Соковитий білий виноград.",
    rating: 4.4,
    isPromo: true,
    oldPrice: 154.0,
  },
  {
    name: "Томати чері свіжі",
    price: 72.53,
    category: "Фрукти, овочі",
    subcategory: "Овочі",
    image: "/images/figma/products/tomatoes.png",
    description: "Свіжі томати чері.",
    rating: 4.2,
    isPromo: true,
    oldPrice: 92.9,
  },
  {
    name: "Банани свіжі",
    price: 45.9,
    category: "Фрукти, овочі",
    subcategory: "Фрукти",
    image: "",
    description: "Стиглі банани.",
    rating: 4.0,
  },
  {
    name: "Яблука Голден свіжі",
    price: 38.9,
    category: "Фрукти, овочі",
    subcategory: "Фрукти",
    image: "",
    description: "Солодкі яблука сорту Голден.",
    rating: 4.1,
  },
  {
    name: "Апельсини свіжі 1кг",
    price: 59.9,
    category: "Фрукти, овочі",
    subcategory: "Фрукти",
    image: "",
    description: "Соковиті апельсини.",
    rating: 4.3,
  },
  {
    name: "Огірки свіжі",
    price: 34.9,
    category: "Фрукти, овочі",
    subcategory: "Овочі",
    image: "",
    description: "Хрусткі свіжі огірки.",
    rating: 4.1,
  },
  {
    name: "Перець болгарський червоний",
    price: 49.9,
    category: "Фрукти, овочі",
    subcategory: "Овочі",
    image: "",
    description: "Соковитий болгарський перець.",
    rating: 4.2,
  },
  {
    name: "Картопля молода 1кг",
    price: 28.9,
    category: "Фрукти, овочі",
    subcategory: "Овочі",
    image: "",
    description: "Свіжа молода картопля.",
    rating: 4.0,
  },
  {
    name: "Малина свіжа 250г",
    price: 89.9,
    category: "Фрукти, овочі",
    subcategory: "Ягоди",
    image: "",
    description: "Соковита свіжа малина.",
    rating: 4.5,
  },
  {
    name: "Чорниця свіжа 250г",
    price: 99.9,
    category: "Фрукти, овочі",
    subcategory: "Ягоди",
    image: "",
    description: "Лісова чорниця.",
    rating: 4.6,
  },
  {
    name: "Лимони свіжі 500г",
    price: 49.9,
    category: "Фрукти, овочі",
    subcategory: "Сезонні овочі, фрукти",
    image: "",
    description: "Стиглі соковиті лимони.",
    rating: 4.3,
  },
  {
    name: "Гарбуз свіжий, частина",
    price: 24.9,
    category: "Фрукти, овочі",
    subcategory: "Сезонні овочі, фрукти",
    image: "",
    description: "Сезонний гарбуз.",
    rating: 4.0,
  },
  {
    name: "Кріп свіжий, пучок",
    price: 19.9,
    category: "Фрукти, овочі",
    subcategory: "Зелень і салати",
    image: "",
    description: "Ароматний свіжий кріп.",
    rating: 4.2,
  },
  {
    name: "Салат Айсберг",
    price: 39.9,
    category: "Фрукти, овочі",
    subcategory: "Зелень і салати",
    image: "",
    description: "Хрусткий салат Айсберг.",
    rating: 4.1,
  },
  {
    name: "Гриби печериці свіжі 500г",
    price: 64.9,
    category: "Фрукти, овочі",
    subcategory: "Гриби",
    image: "",
    description: "Свіжі печериці.",
    rating: 4.3,
  },
  {
    name: "Гриби лисички заморожені 400г",
    price: 119.9,
    category: "Фрукти, овочі",
    subcategory: "Гриби",
    image: "",
    description: "Лісові лисички.",
    rating: 4.4,
  },
  {
    name: "Смузі ягідний 250мл",
    price: 54.9,
    category: "Фрукти, овочі",
    subcategory: "Смузі і фреші",
    image: "",
    description: "Натуральний ягідний смузі.",
    rating: 4.5,
  },
  {
    name: "Фреш апельсиновий 300мл",
    price: 64.9,
    category: "Фрукти, овочі",
    subcategory: "Смузі і фреші",
    image: "",
    description: "Свіжовичавлений апельсиновий сік.",
    rating: 4.4,
  },
  {
    name: "Снеки яблучні сушені 50г",
    price: 44.9,
    category: "Фрукти, овочі",
    subcategory: "Фруктові, овочеві снеки",
    image: "",
    description: "Сушені яблучні чипси.",
    rating: 4.1,
  },
  {
    name: "Горіхи мікс 200г",
    price: 89.9,
    category: "Фрукти, овочі",
    subcategory: "Горіхи і сухофрукти",
    image: "",
    description: "Асорті горіхів.",
    rating: 4.5,
  },
  {
    name: "Курага сушена 200г",
    price: 69.9,
    category: "Фрукти, овочі",
    subcategory: "Горіхи і сухофрукти",
    image: "",
    description: "Натуральна курага.",
    rating: 4.3,
  },
  {
    name: "Огірки солені бочкові 500г",
    price: 54.9,
    category: "Фрукти, овочі",
    subcategory: "Соління",
    image: "",
    description: "Класичні бочкові огірки.",
    rating: 4.4,
  },
  {
    name: "Капуста квашена 500г",
    price: 44.9,
    category: "Фрукти, овочі",
    subcategory: "Соління",
    image: "",
    description: "Домашня квашена капуста.",
    rating: 4.2,
  },

  // ===================== МОЛОЧКА ТА ЯЙЦЯ =====================
  {
    name: "Молоко Галичина 2.5%",
    price: 42.9,
    category: "Молочка та яйця",
    subcategory: "Молоко, вершки",
    image: "",
    description: "Пастеризоване молоко.",
    rating: 4.5,
  },
  {
    name: "Яйця курячі С1, 10 шт",
    price: 64.9,
    category: "Молочка та яйця",
    subcategory: "Яйця",
    image: "",
    description: "Свіжі курячі яйця.",
    rating: 4.3,
  },
  {
    name: "Сметана 20% Президент",
    price: 38.9,
    category: "Молочка та яйця",
    subcategory: "Сметана",
    image: "",
    description: "Густа сметана.",
    rating: 4.2,
  },
  {
    name: "Кефір 1% Яготинський",
    price: 29.9,
    category: "Молочка та яйця",
    subcategory: "Кисломолочні напої",
    image: "",
    description: "Легкий кефір.",
    rating: 4.0,
  },
  {
    name: "Масло вершкове 82.5% 200г",
    price: 89.9,
    category: "Молочка та яйця",
    subcategory: "Масло, маргарин, спред",
    image: "",
    description: "Натуральне вершкове масло.",
    rating: 4.6,
  },
  {
    name: "Йогурт Activia натуральний",
    price: 34.9,
    category: "Молочка та яйця",
    subcategory: "Йогурти, десерти",
    image: "",
    description: "Корисний натуральний йогурт.",
    rating: 4.4,
  },
  {
    name: "Вершки 33% для збивання",
    price: 54.9,
    category: "Молочка та яйця",
    subcategory: "Молоко, вершки",
    image: "",
    description: "Жирні вершки для десертів.",
    rating: 4.5,
  },
  {
    name: "Яйця перепелині, 20 шт",
    price: 54.9,
    category: "Молочка та яйця",
    subcategory: "Яйця",
    image: "",
    description: "Дрібні перепелині яйця.",
    rating: 4.3,
  },
  {
    name: "Сир кисломолочний 9% 400г",
    price: 74.9,
    category: "Молочка та яйця",
    subcategory: "Сир Кисломолочний, сирок",
    image: "",
    description: "Зернистий кисломолочний сир.",
    rating: 4.4,
  },
  {
    name: "Сирки глазуровані шоколадні",
    price: 14.9,
    category: "Молочка та яйця",
    subcategory: "Глазуровані сирки",
    image: "",
    description: "Класичні глазуровані сирки.",
    rating: 4.2,
  },
  {
    name: "Молоко згущене вареное",
    price: 49.9,
    category: "Молочка та яйця",
    subcategory: "Згущене молоко",
    image: "",
    description: "Класична варена згущенка.",
    rating: 4.6,
  },
  {
    name: "Пюре дитяче молочне",
    price: 24.9,
    category: "Молочка та яйця",
    subcategory: "Молочні продукти для дітей",
    image: "",
    description: "Молочне пюре для малюків.",
    rating: 4.3,
  },
  {
    name: "Вареники з сиром, напівфабрикат",
    price: 84.9,
    category: "Молочка та яйця",
    subcategory: "Напівфабрикати власного виробництва",
    image: "",
    description: "Домашні вареники з сиром.",
    rating: 4.4,
  },
  {
    name: "Молоко кокосове рослинне",
    price: 79.9,
    category: "Молочка та яйця",
    subcategory: "Безмолочна продукція",
    image: "",
    description: "Рослинна альтернатива молоку.",
    rating: 4.1,
  },

  // ===================== СИРИ =====================
  {
    name: "Сир Гауда нарізка",
    price: 89.9,
    category: "Сири",
    subcategory: "Тверді і напівтверді сири",
    image: "",
    description: "Ніжний сир Гауда.",
    rating: 4.3,
  },
  {
    name: "Сир Моцарела куля",
    price: 74.9,
    category: "Сири",
    subcategory: "Розсільні сири",
    image: "",
    description: "Свіжа моцарела.",
    rating: 4.6,
  },
  {
    name: "Сир Пармезан тертий 100г",
    price: 129.9,
    category: "Сири",
    subcategory: "Тверді і напівтверді сири",
    image: "",
    description: "Тертий пармезан.",
    rating: 4.7,
  },
  {
    name: "Сир Брі м'який 125г",
    price: 149.9,
    category: "Сири",
    subcategory: "Сири з пліснявою",
    image: "",
    description: "Вершковий сир Брі.",
    rating: 4.4,
  },
  {
    name: "Сир Чеддер нарізка",
    price: 99.9,
    category: "Сири",
    subcategory: "Тверді і напівтверді сири",
    image: "",
    description: "Пікантний сир Чеддер.",
    rating: 4.3,
  },
  {
    name: "Сир Дор Блю з пліснявою",
    price: 169.9,
    category: "Сири",
    subcategory: "Сири з пліснявою",
    image: "",
    description: "Гострий сир з голубою пліснявою.",
    rating: 4.2,
  },
  {
    name: "Сир Фета розсільний 200г",
    price: 84.9,
    category: "Сири",
    subcategory: "Козячі і овечі сири",
    image: "",
    description: "Грецький розсільний сир.",
    rating: 4.5,
  },
  {
    name: "Сир козячий м'який 150г",
    price: 119.9,
    category: "Сири",
    subcategory: "Козячі і овечі сири",
    image: "",
    description: "Ніжний козячий сир.",
    rating: 4.4,
  },
  {
    name: "Сир крем-сир класичний 200г",
    price: 64.9,
    category: "Сири",
    subcategory: "Крем-сири",
    image: "",
    description: "Вершковий крем-сир.",
    rating: 4.5,
  },
  {
    name: "Сир плавлений Дружба 90г",
    price: 19.9,
    category: "Сири",
    subcategory: "Сири плавлені",
    image: "",
    description: "Класичний плавлений сир.",
    rating: 4.0,
  },
  {
    name: "Сирки дитячі творожні",
    price: 29.9,
    category: "Сири",
    subcategory: "Сири для дітей",
    image: "",
    description: "М'які сирки для дітей.",
    rating: 4.3,
  },
  {
    name: "Набір сирів асорті 300г",
    price: 219.9,
    category: "Сири",
    subcategory: "Набори сирів",
    image: "",
    description: "Дегустаційний набір сирів.",
    rating: 4.6,
  },

  // ===================== ХЛІБ ТА ВИПІЧКА =====================
  {
    name: "Хліб Бородинський",
    price: 28.9,
    category: "Хліб та випічка",
    subcategory: "Хлібобулочні вироби",
    image: "",
    description: "Житній хліб.",
    rating: 4.5,
  },
  {
    name: "Батон нарізний",
    price: 22.9,
    category: "Хліб та випічка",
    subcategory: "Хлібобулочні вироби",
    image: "",
    description: "М'який пшеничний батон.",
    rating: 4.2,
  },
  {
    name: "Круасан масляний",
    price: 18.9,
    category: "Хліб та випічка",
    subcategory: "Випічка",
    image: "",
    description: "Листковий круасан.",
    rating: 4.6,
  },
  {
    name: "Булочки для бургера 4 шт",
    price: 34.9,
    category: "Хліб та випічка",
    subcategory: "Хлібобулочні вироби",
    image: "",
    description: "М'які булочки.",
    rating: 4.1,
  },
  {
    name: "Хліб цільнозерновий",
    price: 32.9,
    category: "Хліб та випічка",
    subcategory: "Хлібобулочні вироби",
    image: "",
    description: "Корисний цільнозерновий хліб.",
    rating: 4.4,
  },
  {
    name: "Лаваш тонкий вірменський",
    price: 24.9,
    category: "Хліб та випічка",
    subcategory: "Хлібобулочні вироби",
    image: "",
    description: "Тонкий лаваш.",
    rating: 4.2,
  },
  {
    name: "Коржі для піци 2 шт",
    price: 39.9,
    category: "Хліб та випічка",
    subcategory: "Коржі, основа для піци",
    image: "",
    description: "Готова основа для піци.",
    rating: 4.1,
  },
  {
    name: "Сушка солодка 300г",
    price: 29.9,
    category: "Хліб та випічка",
    subcategory: "Сушка, хлібці пряники",
    image: "",
    description: "Класична сушка з маком.",
    rating: 4.0,
  },
  {
    name: "Хлібці рисові 100г",
    price: 34.9,
    category: "Хліб та випічка",
    subcategory: "Сушка, хлібці пряники",
    image: "",
    description: "Дієтичні рисові хлібці.",
    rating: 4.2,
  },

  // ===================== КОВБАСИ ТА ДЕЛІКАТЕСИ =====================
  {
    name: "Ковбаса Докторська варена",
    price: 94.9,
    category: "Ковбаси та делікатеси",
    subcategory: "Ковбаси",
    image: "",
    description: "Класична варена ковбаса.",
    rating: 4.2,
  },
  {
    name: "Шинка свиняча копчена",
    price: 189.9,
    category: "Ковбаси та делікатеси",
    subcategory: "М'ясні делікатеси",
    image: "",
    description: "Копчена свиняча шинка.",
    rating: 4.5,
  },
  {
    name: "Салямі Мілано нарізка",
    price: 149.9,
    category: "Ковбаси та делікатеси",
    subcategory: "М'ясо-ковбасна нарізка",
    image: "",
    description: "Італійська салямі.",
    rating: 4.4,
  },
  {
    name: "Сосиски Молочні 5 шт",
    price: 64.9,
    category: "Ковбаси та делікатеси",
    subcategory: "Сосиски і сардельки",
    image: "",
    description: "Ніжні молочні сосиски.",
    rating: 4.0,
  },
  {
    name: "Бекон копчений нарізка",
    price: 119.9,
    category: "Ковбаси та делікатеси",
    subcategory: "М'ясо-ковбасна нарізка",
    image: "",
    description: "Ароматний копчений бекон.",
    rating: 4.5,
  },
  {
    name: "Паштет курячий 200г",
    price: 54.9,
    category: "Ковбаси та делікатеси",
    subcategory: "М'ясні делікатеси",
    image: "",
    description: "Ніжний курячий паштет.",
    rating: 4.1,
  },
  {
    name: "Хамон Серрано нарізка 80г",
    price: 249.9,
    category: "Ковбаси та делікатеси",
    subcategory: "Хамон",
    image: "",
    description: "Іспанський хамон серрано.",
    rating: 4.7,
  },
  {
    name: "Хамон Іберіко нарізка 60г",
    price: 399.9,
    category: "Ковбаси та делікатеси",
    subcategory: "Хамон",
    image: "",
    description: "Преміальний хамон іберіко.",
    rating: 4.8,
  },
  {
    name: "Сало копчене з паприкою",
    price: 109.9,
    category: "Ковбаси та делікатеси",
    subcategory: "Сало",
    image: "",
    description: "Гостре копчене сало.",
    rating: 4.4,
  },
  {
    name: "Снеки м'ясні сушені 50г",
    price: 64.9,
    category: "Ковбаси та делікатеси",
    subcategory: "М'ясні снеки",
    image: "",
    description: "Сушене м'ясо снек.",
    rating: 4.2,
  },
  {
    name: "Ковбаски веганські рослинні",
    price: 89.9,
    category: "Ковбаси та делікатеси",
    subcategory: "Веган",
    image: "",
    description: "Рослинна альтернатива ковбасі.",
    rating: 3.9,
  },

  // ===================== ГОТОВІ СТРАВИ І КУЛІНАРІЯ =====================
  {
    name: "Піца Маргарита заморожена",
    price: 129.9,
    category: "Готові страви і кулінарія",
    subcategory: "Суші, піца, бургери",
    image: "",
    description: "Класична піца.",
    rating: 4.1,
  },
  {
    name: "Вареники з картоплею 500г",
    price: 74.9,
    category: "Готові страви і кулінарія",
    subcategory: "Напівфабрикати власного виробництва",
    image: "",
    description: "Домашні вареники.",
    rating: 4.4,
  },
  {
    name: "Котлети курячі заморожені",
    price: 89.9,
    category: "Готові страви і кулінарія",
    subcategory: "Другі страви",
    image: "",
    description: "Соковиті курячі котлети.",
    rating: 4.2,
  },
  {
    name: "Суші-сет Філадельфія 8 шт",
    price: 189.9,
    category: "Готові страви і кулінарія",
    subcategory: "Суші, піца, бургери",
    image: "",
    description: "Класичні роли Філадельфія.",
    rating: 4.6,
  },
  {
    name: "Салат Олів'є 200г",
    price: 64.9,
    category: "Готові страви і кулінарія",
    subcategory: "Салати та закуски",
    image: "",
    description: "Класичний салат Олів'є.",
    rating: 4.0,
  },
  {
    name: "Млинці з м'ясом заморожені",
    price: 79.9,
    category: "Готові страви і кулінарія",
    subcategory: "Млинці, сирники, запіканки",
    image: "",
    description: "Домашні млинці.",
    rating: 4.3,
  },
  {
    name: "Борщ український готовий 500г",
    price: 59.9,
    category: "Готові страви і кулінарія",
    subcategory: "Перші страви",
    image: "",
    description: "Гарячий український борщ.",
    rating: 4.5,
  },
  {
    name: "Суп-крем грибний 400г",
    price: 54.9,
    category: "Готові страви і кулінарія",
    subcategory: "Перші страви",
    image: "",
    description: "Нечний грибний крем-суп.",
    rating: 4.3,
  },
  {
    name: "Сирники з ягодами 300г",
    price: 69.9,
    category: "Готові страви і кулінарія",
    subcategory: "Млинці, сирники, запіканки",
    image: "",
    description: "Готові сирники з підливою.",
    rating: 4.4,
  },
  {
    name: "Тірамісу класичний 150г",
    price: 89.9,
    category: "Готові страви і кулінарія",
    subcategory: "Десерти та напої",
    image: "",
    description: "Італійський десерт тірамісу.",
    rating: 4.6,
  },
  {
    name: "Лимонад домашній 500мл",
    price: 44.9,
    category: "Готові страви і кулінарія",
    subcategory: "Десерти та напої",
    image: "",
    description: "Освіжаючий лимонад.",
    rating: 4.3,
  },
  {
    name: "Сніданок англійський набір",
    price: 99.9,
    category: "Готові страви і кулінарія",
    subcategory: "Сніданки та комплексні обіди",
    image: "",
    description: "Готовий набір для сніданку.",
    rating: 4.2,
  },
  {
    name: "Пиріжки з вишнею 4 шт",
    price: 49.9,
    category: "Готові страви і кулінарія",
    subcategory: "Пироги, пиріжки, випічка",
    image: "",
    description: "Домашні пиріжки з вишнею.",
    rating: 4.5,
  },
  {
    name: "Голубці з м'ясом, напівфабрикат",
    price: 94.9,
    category: "Готові страви і кулінарія",
    subcategory: "Напівфабрикати власного виробництва",
    image: "",
    description: "Готові голубці.",
    rating: 4.3,
  },

  // ===================== БАКАЛІЯ І КОНСЕРВИ =====================
  {
    name: "Рис круглозернистий 1кг",
    price: 44.9,
    category: "Бакалія і консерви",
    subcategory: "Крупи",
    image: "",
    description: "Білий круглозернистий рис.",
    rating: 4.3,
  },
  {
    name: "Макарони Пенне 500г",
    price: 29.9,
    category: "Бакалія і консерви",
    subcategory: "Макаронні вироби",
    image: "",
    description: "Класичні макарони.",
    rating: 4.2,
  },
  {
    name: "Тунець консервований в олії",
    price: 54.9,
    category: "Бакалія і консерви",
    subcategory: "Консервація",
    image: "",
    description: "Ніжний тунець.",
    rating: 4.5,
  },
  {
    name: "Квасоля червона консервована",
    price: 32.9,
    category: "Бакалія і консерви",
    subcategory: "Консервація",
    image: "",
    description: "Готова червона квасоля.",
    rating: 4.0,
  },
  {
    name: "Гречка ядриця 800г",
    price: 49.9,
    category: "Бакалія і консерви",
    subcategory: "Крупи",
    image: "",
    description: "Смачна гречана крупа.",
    rating: 4.5,
  },
  {
    name: "Олія соняшникова 1л",
    price: 59.9,
    category: "Бакалія і консерви",
    subcategory: "Олія та оцет",
    image: "",
    description: "Рафінована соняшникова олія.",
    rating: 4.2,
  },
  {
    name: "Борошно пшеничне вищого сорту 1кг",
    price: 34.9,
    category: "Бакалія і консерви",
    subcategory: "Борошно",
    image: "",
    description: "Якісне пшеничне борошно.",
    rating: 4.3,
  },
  {
    name: "Цукор білий 1кг",
    price: 29.9,
    category: "Бакалія і консерви",
    subcategory: "Сіль, цукор",
    image: "",
    description: "Класичний білий цукор.",
    rating: 4.1,
  },
  {
    name: "Сіль кухонна йодована 1кг",
    price: 14.9,
    category: "Бакалія і консерви",
    subcategory: "Сіль, цукор",
    image: "",
    description: "Йодована кухонна сіль.",
    rating: 4.0,
  },
  {
    name: "Оцет яблучний 500мл",
    price: 39.9,
    category: "Бакалія і консерви",
    subcategory: "Олія та оцет",
    image: "",
    description: "Натуральний яблучний оцет.",
    rating: 4.2,
  },
  {
    name: "Варення вишневе 400г",
    price: 64.9,
    category: "Бакалія і консерви",
    subcategory: "Консервовані фрукти, варення, мед",
    image: "",
    description: "Домашнє вишневе варення.",
    rating: 4.5,
  },
  {
    name: "Мед квітковий натуральний 500г",
    price: 149.9,
    category: "Бакалія і консерви",
    subcategory: "Консервовані фрукти, варення, мед",
    image: "",
    description: "Натуральний квітковий мед.",
    rating: 4.7,
  },
  {
    name: "Локшина швидкого приготування",
    price: 19.9,
    category: "Бакалія і консерви",
    subcategory: "Їжа швидкого приготування",
    image: "",
    description: "Локшина у склянці.",
    rating: 3.8,
  },
  {
    name: "Соус соєвий для суші 200мл",
    price: 49.9,
    category: "Бакалія і консерви",
    subcategory: "Азійська кухня",
    image: "",
    description: "Класичний соус для суші.",
    rating: 4.4,
  },

  // ===================== СОУСИ І СПЕЦІЇ =====================
  {
    name: "Кетчуп томатний Чумак",
    price: 38.9,
    category: "Соуси і спеції",
    subcategory: "Соуси, заправки",
    image: "",
    description: "Класичний томатний кетчуп.",
    rating: 4.3,
  },
  {
    name: "Майонез Торчин 67%",
    price: 44.9,
    category: "Соуси і спеції",
    subcategory: "Соуси, заправки",
    image: "",
    description: "Густий майонез.",
    rating: 4.1,
  },
  {
    name: "Соєвий соус Кіккоман 150мл",
    price: 69.9,
    category: "Соуси і спеції",
    subcategory: "Соуси, заправки",
    image: "",
    description: "Японський соєвий соус.",
    rating: 4.7,
  },
  {
    name: "Перець чорний мелений 50г",
    price: 24.9,
    category: "Соуси і спеції",
    subcategory: "Спеції",
    image: "",
    description: "Ароматний чорний перець.",
    rating: 4.4,
  },
  {
    name: "Гірчиця діжонська 200г",
    price: 49.9,
    category: "Соуси і спеції",
    subcategory: "Соуси, заправки",
    image: "",
    description: "Французька діжонська гірчиця.",
    rating: 4.5,
  },
  {
    name: "Паприка солодка мелена 40г",
    price: 29.9,
    category: "Соуси і спеції",
    subcategory: "Спеції",
    image: "",
    description: "Ароматна солодка паприка.",
    rating: 4.3,
  },
  {
    name: "Ванілін у пакетику 2г",
    price: 9.9,
    category: "Соуси і спеції",
    subcategory: "Все для випічки",
    image: "",
    description: "Натуральний ванілін.",
    rating: 4.0,
  },
  {
    name: "Розпушувач тіста 10г",
    price: 12.9,
    category: "Соуси і спеції",
    subcategory: "Все для випічки",
    image: "",
    description: "Розпушувач для тіста.",
    rating: 4.1,
  },
  {
    name: "Панірувальні сухарі 200г",
    price: 24.9,
    category: "Соуси і спеції",
    subcategory: "Панірування",
    image: "",
    description: "Класичні сухарі для панірування.",
    rating: 4.2,
  },

  // ===================== СОЛОДОЩІ =====================
  {
    name: "Шоколад Roshen молочний",
    price: 54.9,
    category: "Солодощі",
    subcategory: "Шоколад",
    image: "",
    description: "Ніжний молочний шоколад.",
    rating: 4.5,
  },
  {
    name: "Цукерки Рошен асорті 200г",
    price: 89.9,
    category: "Солодощі",
    subcategory: "Цукерки",
    image: "",
    description: "Асорті цукерок.",
    rating: 4.3,
  },
  {
    name: "Печиво Юбілейне 400г",
    price: 44.9,
    category: "Солодощі",
    subcategory: "Печиво, вафлі, бісквіти",
    image: "",
    description: "Класичне вівсяне печиво.",
    rating: 4.1,
  },
  {
    name: "Мармелад фруктовий 300г",
    price: 39.9,
    category: "Солодощі",
    subcategory: "Зефір, мармелад, пастила",
    image: "",
    description: "Натуральний мармелад.",
    rating: 4.0,
  },
  {
    name: "Вафлі Артек з горіховим кремом",
    price: 34.9,
    category: "Солодощі",
    subcategory: "Печиво, вафлі, бісквіти",
    image: "",
    description: "Хрусткі вафлі.",
    rating: 4.2,
  },
  {
    name: "Халва соняшникова 300г",
    price: 49.9,
    category: "Солодощі",
    subcategory: "Східні солодощі",
    image: "",
    description: "Класична соняшникова халва.",
    rating: 4.3,
  },
  {
    name: "Торт Київський 800г",
    price: 249.9,
    category: "Солодощі",
    subcategory: "Власна Кондитерська",
    image: "",
    description: "Легендарний Київський торт.",
    rating: 4.8,
  },
  {
    name: "Тістечко Наполеон, шт",
    price: 39.9,
    category: "Солодощі",
    subcategory: "Власна Кондитерська",
    image: "",
    description: "Шарове тістечко Наполеон.",
    rating: 4.5,
  },
  {
    name: "Зефір ванільний 300г",
    price: 54.9,
    category: "Солодощі",
    subcategory: "Зефір, мармелад, пастила",
    image: "",
    description: "Повітряний ванільний зефір.",
    rating: 4.4,
  },
  {
    name: "Рахат-лукум асорті 200г",
    price: 79.9,
    category: "Солодощі",
    subcategory: "Східні солодощі",
    image: "",
    description: "Східна солодість рахат-лукум.",
    rating: 4.5,
  },
  {
    name: "Жувальна гумка Orbit",
    price: 24.9,
    category: "Солодощі",
    subcategory: "Жувальна гумка",
    image: "",
    description: "М'ятна жувальна гумка.",
    rating: 4.0,
  },

  // ===================== ЗДОРОВЕ ХАРЧУВАННЯ =====================
  {
    name: "Гранола з горіхами 400г",
    price: 89.9,
    category: "Здорове харчування",
    subcategory: "Органічна їжа",
    image: "",
    description: "Корисна гранола.",
    rating: 4.6,
  },
  {
    name: "Мигдаль смажений 150г",
    price: 74.9,
    category: "Здорове харчування",
    subcategory: "Органічна їжа",
    image: "",
    description: "Хрусткий смажений мигдаль.",
    rating: 4.5,
  },
  {
    name: "Йогурт грецький 0% 300г",
    price: 49.9,
    category: "Здорове харчування",
    subcategory: "Дієтичне харчування",
    image: "",
    description: "Натуральний грецький йогурт.",
    rating: 4.4,
  },
  {
    name: "Протеїновий батончик шоколад",
    price: 64.9,
    category: "Здорове харчування",
    subcategory: "Дієтичне харчування",
    image: "",
    description: "Білковий батончик.",
    rating: 4.2,
  },
  {
    name: "Насіння чіа 200г",
    price: 79.9,
    category: "Здорове харчування",
    subcategory: "Органічна їжа",
    image: "",
    description: "Суперфуд насіння чіа.",
    rating: 4.5,
  },
  {
    name: "Авокадо свіже 1 шт",
    price: 54.9,
    category: "Здорове харчування",
    subcategory: "Веганські продукти",
    image: "",
    description: "Стигле авокадо.",
    rating: 4.3,
  },
  {
    name: "Тофу натуральний 300г",
    price: 69.9,
    category: "Здорове харчування",
    subcategory: "Веганські продукти",
    image: "",
    description: "Соєвий тофу.",
    rating: 4.1,
  },
  {
    name: "Молоко безлактозне 1л",
    price: 64.9,
    category: "Здорове харчування",
    subcategory: "Безлактозні продукти",
    image: "",
    description: "Молоко без лактози.",
    rating: 4.3,
  },
  {
    name: "Хліб безглютеновий 300г",
    price: 89.9,
    category: "Здорове харчування",
    subcategory: "Безглютенові продукти",
    image: "",
    description: "Хліб без глютену.",
    rating: 4.0,
  },
  {
    name: "Печиво без цукру 200г",
    price: 79.9,
    category: "Здорове харчування",
    subcategory: "Без доданого цукру",
    image: "",
    description: "Печиво на стевії.",
    rating: 4.2,
  },
];

const recipes = [
  {
    title: "Сирні бейгли",
    image: "/images/figma/recipes/cheese-bagels.jpg",
    description: "Хрусткі домашні бейгли з вершковим сиром.",
  },
  {
    title: "Паста-салат з куркою",
    image: "/images/figma/recipes/pasta-salad.jpg",
    description: "Легкий салат з пастою і курячим філе.",
  },
  {
    title: "Фалафель",
    image: "/images/figma/recipes/falafel.jpg",
    description: "Хрусткі шарики з нуту з ароматними спеціями.",
  },
  {
    title: "Фісташкове тирамісу",
    image: "/images/figma/recipes/pistachio-tiramisu.jpg",
    description: "Італійський десерт з фісташковим присмаком.",
  },
  {
    title: "Відкритий сендвіч з редискою та авокадо",
    image: "/images/figma/recipes/radish-avocado-sandwich.jpg",
    description: "Свіжий і корисний перекус на щодень.",
  },
  {
    title: "Мічелада",
    image: "/images/figma/recipes/michelada.jpg",
    description: "Мексиканський пивний коктейль зі спеціями.",
  },
  {
    title: "Вівчарський пиріг",
    image: "/images/figma/recipes/shepherds-pie.jpg",
    description: "Британська класика з картопляним пюре та м'ясом.",
  },
  {
    title: "Паста з горілкою та лимоном",
    image: "/images/figma/recipes/vodka-lemon-pasta.jpg",
    description: "Вершкова паста з цитрусовими нотками.",
  },
  {
    title: "Віденський шніцель",
    image: "/images/figma/recipes/viennese-schnitzel.jpg",
    description: "Хрустка класика австрійської кухні.",
  },
  {
    title: "Пісний салат з капусти з фініковою заправкою",
    image: "/images/figma/recipes/cabbage-date-salad.jpg",
    description: "Легкий і солодкуватий овочевий салат.",
  },
  {
    title: "Запечена редиска із соусом",
    image: "/images/figma/recipes/baked-radish.jpg",
    description: "Несподіваний гарнір з печеної редиски.",
  },
];

async function main() {
  console.log("🌱 Починаю seed...");

  const existingCount = await prisma.product.count();
  if (existingCount > 0) {
    console.log(`⚠️  В БД вже є ${existingCount} товарів. Пропускаю seed.`);
    console.log("   Якщо хочеш перезаповнити — спочатку запусти:");
    console.log("   node seed.js --force");

    if (!process.argv.includes("--force")) {
      return;
    }

    console.log("🗑️  Очищую таблиці...");
    await prisma.product.deleteMany();
    await prisma.recipe.deleteMany();
  }

  const result = await prisma.product.createMany({ data: products });
  console.log(
    `✅ Додано ${result.count} товарів у ${[...new Set(products.map((p) => p.category))].length} категоріях.`,
  );
  console.log(
    `   Унікальних підкатегорій: ${[...new Set(products.map((p) => p.subcategory).filter(Boolean))].length}`,
  );

  const recipeResult = await prisma.recipe.createMany({ data: recipes });
  console.log(`✅ Додано ${recipeResult.count} рецептів.`);
}

main()
  .catch((e) => {
    console.error("❌ Помилка seed:", e);
    process.exit(1);
  })
  .finally(() => prisma.$disconnect());
</file>

<file path="routes/cart.js">
const express = require("express");
const router = express.Router();
const auth = require("../middleware/authMiddleware");
const { PrismaClient } = require("@prisma/client");
const { PrismaPg } = require("@prisma/adapter-pg");
const { Pool } = require("pg");

const pool = new Pool({ connectionString: process.env.DATABASE_URL });
const adapter = new PrismaPg(pool);
const prisma = new PrismaClient({ adapter });

// Допоміжна функція: підвантажити кошик юзера разом з даними товару
async function loadCart(userId) {
  const items = await prisma.cartItem.findMany({
    where: { userId },
    include: { product: true },
    orderBy: { createdAt: "asc" },
  });
  return items.map((item) => ({
    productId: item.productId,
    quantity: item.quantity,
    product: item.product,
  }));
}

// GET /api/cart — отримати кошик поточного юзера
router.get("/", auth, async (req, res) => {
  try {
    const cart = await loadCart(req.user.userId);
    res.json({ cart });
  } catch (error) {
    console.error("GET CART ERROR:", error);
    res.status(500).json({ error: "Не вдалося завантажити кошик" });
  }
});

// POST /api/cart — додати товар (або збільшити кількість, якщо вже є)
router.post("/", auth, async (req, res) => {
  try {
    const { productId, quantity } = req.body;
    const qty = Number(quantity) || 1;

    if (!productId) {
      return res.status(400).json({ error: "productId обов'язковий" });
    }

    const product = await prisma.product.findUnique({
      where: { id: Number(productId) },
    });
    if (!product) {
      return res.status(404).json({ error: "Товар не знайдено" });
    }

    const existing = await prisma.cartItem.findUnique({
      where: {
        userId_productId: {
          userId: req.user.userId,
          productId: Number(productId),
        },
      },
    });

    if (existing) {
      await prisma.cartItem.update({
        where: { id: existing.id },
        data: { quantity: existing.quantity + qty },
      });
    } else {
      await prisma.cartItem.create({
        data: {
          userId: req.user.userId,
          productId: Number(productId),
          quantity: qty,
        },
      });
    }

    const cart = await loadCart(req.user.userId);
    res.status(201).json({ cart });
  } catch (error) {
    console.error("ADD TO CART ERROR:", error);
    res.status(500).json({ error: "Не вдалося додати товар до кошика" });
  }
});

// PUT /api/cart/:productId — оновити кількість
router.put("/:productId", auth, async (req, res) => {
  try {
    const productId = Number(req.params.productId);
    const quantity = Number(req.body.quantity);

    if (!quantity || quantity < 1) {
      return res.status(400).json({ error: "quantity має бути більше 0" });
    }

    const existing = await prisma.cartItem.findUnique({
      where: { userId_productId: { userId: req.user.userId, productId } },
    });
    if (!existing) {
      return res.status(404).json({ error: "Товар не знайдено в кошику" });
    }

    await prisma.cartItem.update({
      where: { id: existing.id },
      data: { quantity },
    });

    const cart = await loadCart(req.user.userId);
    res.json({ cart });
  } catch (error) {
    console.error("UPDATE CART ITEM ERROR:", error);
    res.status(500).json({ error: "Не вдалося оновити кошик" });
  }
});

// DELETE /api/cart/:productId — видалити один товар
router.delete("/:productId", auth, async (req, res) => {
  try {
    const productId = Number(req.params.productId);

    await prisma.cartItem.deleteMany({
      where: { userId: req.user.userId, productId },
    });

    const cart = await loadCart(req.user.userId);
    res.json({ cart });
  } catch (error) {
    console.error("REMOVE FROM CART ERROR:", error);
    res.status(500).json({ error: "Не вдалося видалити товар з кошика" });
  }
});

// DELETE /api/cart — очистити весь кошик
router.delete("/", auth, async (req, res) => {
  try {
    await prisma.cartItem.deleteMany({ where: { userId: req.user.userId } });
    res.json({ cart: [] });
  } catch (error) {
    console.error("CLEAR CART ERROR:", error);
    res.status(500).json({ error: "Не вдалося очистити кошик" });
  }
});

module.exports = router;
</file>

<file path="scripts/linkNewProductPhotos.js">
/**
 * Прив'язує нові фото товарів (додані колегою в frontend/public/images/figma/products)
 * до товарів у БД, у яких image = null, і одразу завантажує ці фото в Cloudinary
 * (щоб не порушувати вимогу "не тримати фото товарів локально").
 *
 * ВАЖЛИВО: скрипт зіставляє файл із товаром за ключовими словами в назві —
 * автоматичний підбір може помилятись, тому за замовчуванням це DRY RUN:
 * він лише ВИВОДИТЬ таблицю передбачуваних відповідностей і НІЧОГО не змінює.
 *
 * Крок 1 — перевірка (нічого не змінює в БД/Cloudinary):
 *   node scripts/linkNewProductPhotos.js
 *
 * Крок 2 — після того, як візуально перевірили список і всі відповідності
 * виглядають правильно, застосувати реально:
 *   node scripts/linkNewProductPhotos.js --apply
 *
 * Джерело файлів — та сама тимчасова папка всередині контейнера, куди
 * копіюєте картинки командою docker cp (як і минулого разу):
 *   docker exec silpo_backend mkdir -p /tmp/migrate-images
 *   docker cp frontend\public\images\figma\products silpo_backend:/tmp/migrate-images/products
 */

require("dotenv").config();
const fs = require("fs");
const path = require("path");
const cloudinary = require("cloudinary").v2;
const { PrismaClient } = require("@prisma/client");
const { PrismaPg } = require("@prisma/adapter-pg");
const { Pool } = require("pg");

const pool = new Pool({ connectionString: process.env.DATABASE_URL });
const adapter = new PrismaPg(pool);
const prisma = new PrismaClient({ adapter });

cloudinary.config({
  cloud_name: process.env.CLOUDINARY_CLOUD_NAME,
  api_key: process.env.CLOUDINARY_API_KEY,
  api_secret: process.env.CLOUDINARY_API_SECRET,
});

// Джерело файлів. Дві можливі ситуації:
// 1) Скрипт запущено всередині Docker-контейнера бекенду (docker exec) —
//    тоді файли мають бути заздалегідь скопійовані туди через docker cp
//    у /tmp/migrate-images/products.
// 2) Скрипт запущено напряму на хості через `railway run` (щоб мати доступ
//    до продакшн-БД/Cloudinary, але виконання відбувається на вашій
//    машині) — тоді простіше й надійніше брати файли напряму з
//    frontend/public/images/figma/products відносним шляхом.
const DOCKER_SOURCE_DIR = "/tmp/migrate-images/products";
const HOST_SOURCE_DIR = path.join(
  __dirname,
  "..",
  "..",
  "frontend",
  "public",
  "images",
  "figma",
  "products",
);
const SOURCE_DIR = fs.existsSync(DOCKER_SOURCE_DIR)
  ? DOCKER_SOURCE_DIR
  : HOST_SOURCE_DIR;
const CLOUD_FOLDER = "kalpo-shop/products";
const APPLY = process.argv.includes("--apply");

// Ім'я файлу (без розширення) -> ключові слова, які мають зустрічатись
// в назві товару (регістронезалежно), щоб вважати це відповідністю.
const FILE_KEYWORDS = {
  brie: ["брі"],
  cheddar: ["чеддер"],
  mozzarella: ["моцарел"],
  parmesan: ["пармезан"],
  gouda: ["гауда"],
  feta: ["фета"],
  "dor-blue": ["дор блю", "дорблю"],
  "goat-cheese": ["козячий сир", "козячого сиру"],
  "cottage-cheese": ["творог", "творож"],
  "cream-cheese": ["вершковий сир"],
  "glazed-curds": ["сирок глазуров", "глазуров"],
  butter: ["масло вершков"],
  "sour-cream": ["сметана"],
  milk: ["молоко"],
  kefir: ["кефір"],
  cream: ["вершки"],
  eggs: ["яйця курячі"],
  "quail-eggs": ["перепелині яйця"],
  yogurt: ["йогурт"],
  "condensed-milk": ["згущене молоко", "згущенка"],
  honey: ["мед"],
  sugar: ["цукор"],
  salt: ["сіль"],
  flour: ["борошно"],
  rice: ["рис"],
  buckwheat: ["гречка", "гречан"],
  "red-beans": ["квасоля"],
  penne: ["пенне"],
  "sunflower-oil": ["олія соняшник"],
  mayonnaise: ["майонез"],
  ketchup: ["кетчуп"],
  "canned-tuna": ["тунець"],
  lavash: ["лаваш"],
  "pizza-dough": ["тісто для піци"],
  "burger-buns": ["булочки для бургер"],
  "sliced-loaf": ["нарізний батон", "хліб нарізний"],
  "whole-grain-bread": ["цільнозернов"],
  "borodinsky-bread": ["бородинськ"],
  croissant: ["круасан"],
  "artek-waffles": ["артек"],
  "jubilee-cookies": ["ювілейне"],
  marmelad: ["мармелад"],
  marshmallow: ["зефір"],
  halva: ["халва"],
  "kyiv-cake": ["київський торт"],
  "roshen-candies": ["рошен"],
  "roshen-chocolate": ["рошен"],
  zewa: ["туалетний папір"],
  nivea_creme: ["nivea", "крем для"],
  orbit: ["orbit", "жувальн"],
};

const IMAGE_EXT = /\.(jpe?g|png|webp)$/i;

// Перевіряє, що keyword зустрічається в тексті як окреме слово/фраза,
// а не як підрядок усередині іншого слова (напр. "брі" не повинно
// зловитись усередині "скумбрія", "сіль" — усередині "розсільний").
const CYRILLIC_LETTER = /[а-яіїєґА-ЯІЇЄҐ]/;
function containsWord(text, keyword) {
  const idx = text.indexOf(keyword);
  if (idx === -1) return false;
  const before = text[idx - 1];
  const after = text[idx + keyword.length];
  if (before && CYRILLIC_LETTER.test(before)) return false;
  if (after && CYRILLIC_LETTER.test(after)) return false;
  return true;
}

async function uploadFile(absPath) {
  const publicId = path.basename(absPath).replace(IMAGE_EXT, "");
  const result = await cloudinary.uploader.upload(absPath, {
    folder: CLOUD_FOLDER,
    public_id: publicId,
    overwrite: false,
    resource_type: "image",
  });
  return result.secure_url;
}

async function main() {
  console.log(`Джерело фото: ${SOURCE_DIR}\n`);
  if (!fs.existsSync(SOURCE_DIR)) {
    console.error(`Немає папки ${SOURCE_DIR}. Спочатку docker cp туди фото.`);
    process.exit(1);
  }

  const products = await prisma.product.findMany({
    where: { OR: [{ image: null }, { image: "" }] },
  });
  console.log(`Товарів без фото в БД: ${products.length}\n`);

  const files = fs.readdirSync(SOURCE_DIR).filter((f) => IMAGE_EXT.test(f));

  const matches = [];
  const unmatchedFiles = [];
  const usedProductIds = new Set();

  for (const file of files) {
    const base = file.replace(IMAGE_EXT, "");
    const keywords = FILE_KEYWORDS[base];
    if (!keywords) {
      unmatchedFiles.push(file);
      continue;
    }
    const found = products.find(
      (p) =>
        !usedProductIds.has(p.id) &&
        keywords.some((kw) =>
          containsWord(p.name.toLowerCase(), kw.toLowerCase()),
        ),
    );
    if (found) {
      matches.push({ file, product: found });
      usedProductIds.add(found.id);
    } else {
      unmatchedFiles.push(file);
    }
  }

  console.log("=== ЗНАЙДЕНІ ВІДПОВІДНОСТІ ===");
  matches.forEach((m) =>
    console.log(`  ${m.file}  ->  [id ${m.product.id}] ${m.product.name}`),
  );

  console.log("\n=== ФАЙЛИ БЕЗ ПАРИ (перевірте вручну) ===");
  unmatchedFiles.forEach((f) => console.log(`  ${f}`));

  console.log(
    `\nВсього: ${matches.length} відповідностей, ${unmatchedFiles.length} без пари.`,
  );

  if (!APPLY) {
    console.log(
      "\n(Це dry-run, нічого не змінено. Перевірте список вище і запустіть з --apply.)",
    );
    await prisma.$disconnect();
    return;
  }

  console.log("\nЗастосовую: завантажую в Cloudinary і оновлюю БД...\n");
  for (const { file, product } of matches) {
    try {
      const url = await uploadFile(path.join(SOURCE_DIR, file));
      await prisma.product.update({
        where: { id: product.id },
        data: { image: url },
      });
      console.log(`  OK [id ${product.id}] ${product.name} -> ${url}`);
    } catch (err) {
      console.error(`  ERROR ${file}:`, err.message);
    }
  }

  console.log("\nГотово.");
  await prisma.$disconnect();
}

main().catch(async (err) => {
  console.error("Failed:", err);
  await prisma.$disconnect();
  process.exit(1);
});
</file>

<file path="scripts/migrateImagesToCloudinary.js">
/**
 * Одноразовий скрипт міграції: завантажує статичні демо-фото товарів і
 * рецептів (які зараз лежать у frontend/public/images/figma/...) у Cloudinary,
 * а потім оновлює поля `image` в таблицях Product і Recipe на нові URL.
 *
 * Навіщо: ці фото зараз "запечені" у Docker-образ фронтенду (COPY . .),
 * тобто роздуваю образ і не мають CDN/оптимізації. Cloudinary вже
 * підключений і використовується для фото, які завантажує адмін —
 * логічно перенести туди і решту.
 *
 * Запуск (з папки backend, де є .env з CLOUDINARY_* та DATABASE_URL):
 *   node scripts/migrateImagesToCloudinary.js
 *
 * Скрипт ідемпотентний: якщо запустити повторно, він пропустить записи,
 * які вже вказують на res.cloudinary.com.
 */

require("dotenv").config();
const fs = require("fs");
const path = require("path");
const cloudinary = require("cloudinary").v2;
const { PrismaClient } = require("@prisma/client");
const { PrismaPg } = require("@prisma/adapter-pg");
const { Pool } = require("pg");

const pool = new Pool({ connectionString: process.env.DATABASE_URL });
const adapter = new PrismaPg(pool);
const prisma = new PrismaClient({ adapter });

cloudinary.config({
  cloud_name: process.env.CLOUDINARY_CLOUD_NAME,
  api_key: process.env.CLOUDINARY_API_KEY,
  api_secret: process.env.CLOUDINARY_API_SECRET,
});

// Джерело: тимчасова папка всередині контейнера, куди ви заздалегідь
// скопіювали зображення командою docker cp (бекенд-контейнер не бачить
// папку frontend напряму — це окремий Docker-образ з іншим build-контекстом).
const SOURCE_DIR = "/tmp/migrate-images";

const FOLDERS_TO_MIGRATE = [
  {
    localDir: "products",
    dbPrefix: "/images/figma/products",
    cloudFolder: "kalpo-shop/products",
  },
  {
    localDir: "recipes",
    dbPrefix: "/images/figma/recipes",
    cloudFolder: "kalpo-shop/recipes",
  },
];

const IMAGE_EXT = /\.(jpe?g|png|webp)$/i;

function walk(dir) {
  let results = [];
  for (const entry of fs.readdirSync(dir, { withFileTypes: true })) {
    const full = path.join(dir, entry.name);
    if (entry.isDirectory()) {
      results = results.concat(walk(full));
    } else if (IMAGE_EXT.test(entry.name)) {
      results.push(full);
    }
  }
  return results;
}

async function uploadFile(absPath, cloudFolder) {
  const publicId = path.basename(absPath).replace(IMAGE_EXT, "");
  const result = await cloudinary.uploader.upload(absPath, {
    folder: cloudFolder,
    public_id: publicId,
    overwrite: false,
    resource_type: "image",
  });
  return result.secure_url;
}

async function main() {
  if (
    !process.env.CLOUDINARY_CLOUD_NAME ||
    !process.env.CLOUDINARY_API_KEY ||
    !process.env.CLOUDINARY_API_SECRET
  ) {
    console.error(
      "Немає CLOUDINARY_CLOUD_NAME / CLOUDINARY_API_KEY / CLOUDINARY_API_SECRET у .env. Зупиняюсь.",
    );
    process.exit(1);
  }

  // localPath (як в БД, напр. "/images/figma/products/fish/fish-2.jpg") -> cloudinary URL
  const mapping = {};

  for (const { localDir, dbPrefix, cloudFolder } of FOLDERS_TO_MIGRATE) {
    const absDir = path.join(SOURCE_DIR, localDir);
    if (!fs.existsSync(absDir)) {
      console.warn(`Пропускаю — папки немає: ${absDir}`);
      continue;
    }

    const files = walk(absDir);
    console.log(`\n📁 ${localDir}: знайдено ${files.length} файлів`);

    for (const absPath of files) {
      const relative = path.relative(absDir, absPath).split(path.sep).join("/");
      const dbPath = `${dbPrefix}/${relative}`;

      try {
        const url = await uploadFile(absPath, cloudFolder);
        mapping[dbPath] = url;
        console.log(`  ✔ ${dbPath} -> ${url}`);
      } catch (err) {
        console.error(`  ✘ Помилка завантаження ${dbPath}:`, err.message);
      }
    }
  }

  console.log(
    `\nЗавантажено ${Object.keys(mapping).length} фото в Cloudinary.`,
  );
  console.log("Оновлюю посилання в базі даних...\n");

  let updatedProducts = 0;
  let updatedRecipes = 0;

  const products = await prisma.product.findMany();
  for (const product of products) {
    if (product.image && mapping[product.image]) {
      await prisma.product.update({
        where: { id: product.id },
        data: { image: mapping[product.image] },
      });
      updatedProducts++;
    }
  }

  const recipes = await prisma.recipe.findMany();
  for (const recipe of recipes) {
    if (recipe.image && mapping[recipe.image]) {
      await prisma.recipe.update({
        where: { id: recipe.id },
        data: { image: mapping[recipe.image] },
      });
      updatedRecipes++;
    }
  }

  console.log(`Оновлено товарів: ${updatedProducts}`);
  console.log(`Оновлено рецептів: ${updatedRecipes}`);
  console.log(
    "\nГотово. Тепер можна видалити локальні файли з frontend/public/images/figma/products та /recipes.",
  );

  await prisma.$disconnect();
}

main().catch(async (err) => {
  console.error("Migration failed:", err);
  await prisma.$disconnect();
  process.exit(1);
});
</file>

<file path=".gitignore">
node_modules
# Keep environment variables out of version control
.env

/generated/prisma

node_modules/

node_modules/
.env
dist/
.prisma/
</file>

<file path="check-users.js">
require('dotenv').config();
const { PrismaClient } = require('@prisma/client');
const { PrismaPg } = require('@prisma/adapter-pg');
const { Pool } = require('pg');

const pool = new Pool({ connectionString: process.env.DATABASE_URL });
const adapter = new PrismaPg(pool);
const p = new PrismaClient({ adapter });

p.user.findMany({ select: { id: true, email: true, isAdmin: true } })
  .then(r => console.log(JSON.stringify(r, null, 2)))
  .catch(e => console.error(e))
  .finally(() => p.$disconnect());
</file>

<file path="Dockerfile">
# 1. Легкий Linux із Node.js
FROM node:22-alpine

# 2. Робоча папка для бекенду
WORKDIR /app

# 3. Копіюємо package.json та встановлюємо ТІЛЬКИ production-бібліотеки
COPY package*.json ./
RUN npm install --omit=dev

# 4. Копіюємо весь source-код бекенду
COPY . .
# 5. Генеруємо Prisma Client зі свіжої схеми (обов'язково після кожної зміни schema.prisma)
RUN npx prisma generate
# 6. Відкриваємо порт бекенду
EXPOSE 3000
# 7. Запускаємо production-сервер
CMD ["npm", "run", "start"]
</file>

<file path="make-admin.js">
require('dotenv').config();
const { PrismaClient } = require('@prisma/client');
const { PrismaPg } = require('@prisma/adapter-pg');
const { Pool } = require('pg');

const pool = new Pool({ connectionString: process.env.DATABASE_URL });
const adapter = new PrismaPg(pool);
const p = new PrismaClient({ adapter });

const email = process.argv[2];

if (!email) {
  console.error('Вкажи email: node make-admin.js твій@email.com');
  process.exit(1);
}

p.user.update({ where: { email }, data: { isAdmin: true } })
  .then(u => console.log(`✅ ${u.email} тепер адмін!`))
  .catch(e => console.error('Помилка:', e.message))
  .finally(() => p.$disconnect());
</file>

<file path="package.json">
{
  "name": "backend",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "dev": "nodemon server.js",
    "start": "node server.js",
    "seed": "node prisma/seed.js",
    "seed:force": "node prisma/seed.js --force"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@prisma/adapter-pg": "^7.8.0",
    "@prisma/client": "^7.8.0",
    "bcrypt": "^6.0.0",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "multer": "^1.4.5-lts.1",
    "nodemailer": "^6.10.1",
    "pg": "^8.20.0",
    "cloudinary": "^2.5.1",
    "stripe": "^17.7.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.14",
    "prisma": "^7.8.0"
  }
}
</file>

<file path="prisma.config.js">
require('dotenv').config();
const path = require('node:path');
const { defineConfig } = require('prisma/config');
module.exports = defineConfig({
  earlyAccess: true,
  schema: path.join('prisma', 'schema.prisma'),
  datasource: {
    url: process.env.DATABASE_URL,
  },
  migrate: {
    async adapter() {
      const { PrismaPg } = require('@prisma/adapter-pg');
      const { Pool } = require('pg');
      const pool = new Pool({ connectionString: process.env.DATABASE_URL });
      return new PrismaPg(pool);
    },
  },
});
</file>

<file path="script.js">
let cartCount = 0;
const buttons = document.querySelectorAll(".product-card button");
const cartButton = document.querySelector(".user-actions button:last-child");

buttons.forEach((button) => {
  button.addEventListener("click", () => {
    cartCount++;
    cartButton.textContent = `Кошик (${cartCount})`;
  });
});
</file>

<file path="server.js">
require("dotenv").config();

const express = require("express");
const cors = require("cors");
const bcrypt = require("bcrypt");
const jwt = require("jsonwebtoken");
const crypto = require("crypto");
const { PrismaClient } = require("@prisma/client");
const { PrismaPg } = require("@prisma/adapter-pg");
const { Pool } = require("pg");

const cloudinary = require("cloudinary").v2;
const multer = require("multer");
const nodemailer = require("nodemailer");
const stripe = require("stripe")(process.env.STRIPE_SECRET_KEY);

const pool = new Pool({ connectionString: process.env.DATABASE_URL });
const adapter = new PrismaPg(pool);
const prisma = new PrismaClient({ adapter });

const app = express();
app.use(cors({ origin: true, credentials: true }));

// Stripe webhook ПОВИНЕН йти ДО express.json(),
// бо Stripe вимагає сирий (raw) body для перевірки підпису
app.post(
  "/api/payments/webhook",
  express.raw({ type: "application/json" }),
  async (req, res) => {
    const sig = req.headers["stripe-signature"];
    let event;

    try {
      event = stripe.webhooks.constructEvent(
        req.body,
        sig,
        process.env.STRIPE_WEBHOOK_SECRET,
      );
    } catch (err) {
      return res.status(400).send(`Webhook Error: ${err.message}`);
    }

    if (event.type === "checkout.session.completed") {
      const session = event.data.object;
      const orderId = session.metadata?.orderId;

      if (orderId) {
        await prisma.order
          .update({
            where: { id: Number(orderId) },
            data: { status: "paid" },
          })
          .catch((err) =>
            console.error("WEBHOOK: не вдалося оновити замовлення", err),
          );
      }
    }

    res.json({ received: true });
  },
);

app.use(express.json());

const JWT_SECRET = process.env.JWT_SECRET;
const REFRESH_SECRET =
  process.env.REFRESH_SECRET || process.env.JWT_SECRET + "_refresh";
const JWT_EXPIRES_IN = "15m";
const REFRESH_EXPIRES_IN = "7d";

const cartRoutes = require("./routes/cart");
const authMiddleware = require("./middleware/authMiddleware");
app.use("/api/cart", cartRoutes);

// ==========================================
// MIDDLEWARE: тільки для адміна
// ==========================================

function adminMiddleware(req, res, next) {
  if (!req.user?.isAdmin) {
    return res
      .status(403)
      .json({ error: "Доступ заборонено. Потрібні права адміна." });
  }
  next();
}

// ==========================================
// HELPERS — генерація токенів
// ==========================================

function generateAccessToken(user) {
  return jwt.sign(
    { userId: user.id, email: user.email, isAdmin: user.isAdmin },
    JWT_SECRET,
    { expiresIn: JWT_EXPIRES_IN },
  );
}

function generateRefreshToken(user) {
  return jwt.sign({ userId: user.id, email: user.email }, REFRESH_SECRET, {
    expiresIn: REFRESH_EXPIRES_IN,
  });
}

// ==========================================
// DTO — форматування відповідей API
// Повертаємо тільки потрібні фронтенду поля,
// ніколи не віддаємо пароль чи внутрішні службові поля.
// ==========================================

// Форматує Date у рядок дд.мм.рррр для фронтенду
function formatDateForFrontend(date) {
  if (!date) return null;
  const d = new Date(date);
  if (Number.isNaN(d.getTime())) return null;
  const day = String(d.getUTCDate()).padStart(2, "0");
  const month = String(d.getUTCMonth() + 1).padStart(2, "0");
  const year = d.getUTCFullYear();
  return `${day}.${month}.${year}`;
}

function formatUser(user) {
  return {
    id: user.id,
    email: user.email,
    firstName: user.firstName,
    lastName: user.lastName,
    middleName: user.middleName || null,
    phone: user.phone || null,
    birthDate: formatDateForFrontend(user.birthDate),
    gender: user.gender || null,
    address: user.address || null,
    isAdmin: user.isAdmin,
  };
}

function formatUserBrief(user) {
  return {
    id: user.id,
    email: user.email,
    firstName: user.firstName,
    lastName: user.lastName,
    isAdmin: user.isAdmin,
    createdAt: user.createdAt,
  };
}

function formatProduct(product) {
  return {
    id: product.id,
    name: product.name,
    description: product.description || null,
    price: product.price,
    oldPrice: product.oldPrice || null,
    category: product.category,
    subcategory: product.subcategory || null,
    image: product.image || null,
    images: (product.images || [])
      .sort((a, b) => a.order - b.order)
      .map((img) => img.url),
    rating: product.rating,
    isPromo: product.isPromo,
  };
}

function formatReview(review) {
  return {
    id: review.id,
    rating: review.rating,
    comment: review.comment || null,
    createdAt: review.createdAt,
    user: review.user
      ? { firstName: review.user.firstName, lastName: review.user.lastName }
      : null,
  };
}

function formatProductWithReviews(product) {
  return {
    ...formatProduct(product),
    reviews: (product.reviews || []).map(formatReview),
  };
}

function formatRecipe(recipe) {
  return {
    id: recipe.id,
    title: recipe.title,
    image: recipe.image || null,
    description: recipe.description || null,
  };
}

function formatOrderItem(item) {
  return {
    id: item.id,
    productId: item.productId,
    name: item.name,
    price: item.price,
    quantity: item.quantity,
  };
}

function formatOrder(order) {
  const dto = {
    id: order.id,
    total: order.total,
    address: order.address,
    status: order.status,
    paymentMethod: order.paymentMethod,
    comment: order.comment || null,
    createdAt: order.createdAt,
    items: (order.items || []).map(formatOrderItem),
  };
  if (order.user) {
    dto.user = {
      email: order.user.email,
      firstName: order.user.firstName,
      lastName: order.user.lastName,
    };
  }
  return dto;
}

// ==========================================
// РЕЄСТРАЦІЯ
// ==========================================

app.post("/api/auth/register", async (req, res) => {
  try {
    const { email, password, firstName, lastName } = req.body;

    if (!email) return res.status(400).json({ error: "Email обов'язковий" });
    if (!password)
      return res.status(400).json({ error: "Пароль обов'язковий" });
    if (password.length < 6)
      return res.status(400).json({ error: "Пароль мінімум 6 символів" });

    const existingUser = await prisma.user.findUnique({ where: { email } });
    if (existingUser)
      return res
        .status(409)
        .json({ error: "Користувач з таким email вже існує" });

    const hashedPassword = await bcrypt.hash(password, 10);
    const user = await prisma.user.create({
      data: { email, password: hashedPassword, firstName, lastName },
    });

    const accessToken = generateAccessToken(user);
    const refreshToken = generateRefreshToken(user);

    res.status(201).json({
      message: "Реєстрація успішна",
      accessToken,
      refreshToken,
      user: formatUser(user),
    });
  } catch (error) {
    console.error("REGISTER ERROR:", error);
    res.status(500).json({ error: "Помилка сервера при реєстрації" });
  }
});

// ==========================================
// ЛОГІН
// ==========================================

app.post("/api/auth/login", async (req, res) => {
  try {
    const { email, password } = req.body;

    if (!email || !password)
      return res.status(400).json({ error: "Email та пароль обов'язкові" });

    const user = await prisma.user.findUnique({ where: { email } });
    if (!user)
      return res.status(401).json({ error: "Невірний email або пароль" });

    const validPassword = await bcrypt.compare(password, user.password);
    if (!validPassword)
      return res.status(401).json({ error: "Невірний email або пароль" });

    const accessToken = generateAccessToken(user);
    const refreshToken = generateRefreshToken(user);

    res.json({
      message: "Успішний вхід",
      accessToken,
      refreshToken,
      user: formatUser(user),
    });
  } catch (error) {
    console.error("LOGIN ERROR:", error);
    res.status(500).json({ error: "Помилка сервера" });
  }
});

// ==========================================
// REFRESH TOKEN (JWT, без зберігання в БД)
// ==========================================

app.post("/api/auth/refresh", async (req, res) => {
  try {
    const { refreshToken } = req.body;
    if (!refreshToken)
      return res.status(400).json({ error: "Refresh token відсутній" });

    let payload;
    try {
      payload = jwt.verify(refreshToken, REFRESH_SECRET);
    } catch {
      return res
        .status(401)
        .json({ error: "Невалідний або протермінований refresh token" });
    }

    const user = await prisma.user.findUnique({
      where: { id: payload.userId },
    });
    if (!user)
      return res.status(401).json({ error: "Користувача не знайдено" });

    const newAccessToken = generateAccessToken(user);
    const newRefreshToken = generateRefreshToken(user);

    res.json({ accessToken: newAccessToken, refreshToken: newRefreshToken });
  } catch (error) {
    console.error("REFRESH ERROR:", error);
    res.status(500).json({ error: "Помилка оновлення токена" });
  }
});

// ==========================================
// LOGOUT
// ==========================================

app.post("/api/auth/logout", async (req, res) => {
  // JWT refresh токени не зберігаються в БД,
  // тому logout просто повідомляє клієнту видалити токени
  res.json({ message: "Вихід виконано" });
});

// ==========================================
// FORGOT PASSWORD (реальна відправка email)
// ==========================================

const transporter = nodemailer.createTransport({
  host: process.env.EMAIL_HOST,
  port: Number(process.env.EMAIL_PORT),
  auth: {
    user: process.env.EMAIL_USER,
    pass: process.env.EMAIL_PASS,
  },
});

// ==========================================
// SMS-ВІДПРАВКА (підтвердження телефону)
// ==========================================
// Реального SMS-провайдера (Twilio тощо) не підключено — потрібні власні
// облікові дані. Поки що код лише логується в консоль бекенду (dev-режим),
// щоб фронтенд і QA могли протестувати весь флоу без реального SMS.
// Щоб підключити Twilio: npm install twilio, додати в .env
// TWILIO_ACCOUNT_SID / TWILIO_AUTH_TOKEN / TWILIO_PHONE_NUMBER,
// і розкоментувати блок нижче.
async function sendSms(phone, code) {
  if (
    process.env.TWILIO_ACCOUNT_SID &&
    process.env.TWILIO_AUTH_TOKEN &&
    process.env.TWILIO_PHONE_NUMBER
  ) {
    const twilio = require("twilio")(
      process.env.TWILIO_ACCOUNT_SID,
      process.env.TWILIO_AUTH_TOKEN,
    );
    await twilio.messages.create({
      body: `Код підтвердження Kalpo: ${code}`,
      from: process.env.TWILIO_PHONE_NUMBER,
      to: phone,
    });
    return;
  }
  console.log(`[DEV SMS] Код підтвердження для ${phone}: ${code}`);
}

const SMS_CODE_TTL_MS = 5 * 60 * 1000; // 5 хвилин

app.post("/api/auth/send-sms", authMiddleware, async (req, res) => {
  const { phone } = req.body;
  if (!phone || !/^\+?\d{9,15}$/.test(phone.replace(/[\s()-]/g, ""))) {
    return res.status(400).json({ error: "Вкажіть коректний номер телефону" });
  }

  try {
    const code = String(Math.floor(100000 + Math.random() * 900000));
    const expiresAt = new Date(Date.now() + SMS_CODE_TTL_MS);

    await prisma.smsVerificationCode.create({
      data: { phone, code, expiresAt },
    });

    await sendSms(phone, code);

    res.json({
      message: "Код надіслано",
      // Код повертається в відповіді лише поза продакшн-середовищем,
      // щоб можна було тестувати без реального SMS-провайдера.
      ...(process.env.NODE_ENV !== "production" ? { devCode: code } : {}),
    });
  } catch (error) {
    console.error("SEND SMS ERROR:", error);
    res.status(500).json({ error: "Не вдалося надіслати SMS" });
  }
});

app.post("/api/auth/verify-sms", authMiddleware, async (req, res) => {
  const { phone, code } = req.body;
  if (!phone || !code) {
    return res.status(400).json({ error: "Телефон і код обов'язкові" });
  }

  try {
    const record = await prisma.smsVerificationCode.findFirst({
      where: { phone, code, used: false },
      orderBy: { createdAt: "desc" },
    });

    if (!record) {
      return res.status(400).json({ error: "Невірний код" });
    }

    if (record.expiresAt < new Date()) {
      return res
        .status(400)
        .json({ error: "Код протермінований, запросіть новий" });
    }

    await prisma.smsVerificationCode.update({
      where: { id: record.id },
      data: { used: true },
    });

    const updatedUser = await prisma.user.update({
      where: { id: req.user.userId },
      data: { phone },
    });

    res.json({
      message: "Телефон підтверджено",
      user: formatUser(updatedUser),
    });
  } catch (error) {
    console.error("VERIFY SMS ERROR:", error);
    res.status(500).json({ error: "Не вдалося перевірити код" });
  }
});

app.post("/api/auth/forgot-password", async (req, res) => {
  const { email } = req.body;
  if (!email) return res.status(400).json({ error: "Email обов'язковий" });

  try {
    const user = await prisma.user.findUnique({ where: { email } });

    if (!user) {
      return res.json({
        message: "Якщо такий email існує, ми надішлемо інструкції",
      });
    }

    await prisma.passwordResetToken.deleteMany({ where: { userId: user.id } });

    const resetToken = crypto.randomBytes(32).toString("hex");
    const expiresAt = new Date(Date.now() + 60 * 60 * 1000); // 1 година

    await prisma.passwordResetToken.create({
      data: { token: resetToken, userId: user.id, expiresAt },
    });

    await transporter.sendMail({
      from: `"Kalpo Shop" <${process.env.EMAIL_USER}>`,
      to: email,
      subject: "Відновлення паролю — Kalpo",
      html: `
        <div style="font-family: Arial, sans-serif; max-width: 480px; margin: 0 auto; padding: 32px;">
          <h2 style="color: #8E1616;">Відновлення паролю</h2>
          <p>Ви отримали цей лист тому що хтось запросив скидання паролю для вашого акаунту.</p>
          <p>Натисніть кнопку нижче щоб встановити новий пароль:</p>
          <a href="${process.env.FRONTEND_URL}/reset-password?token=${resetToken}"
             style="display: inline-block; padding: 12px 24px; background: #8E1616; color: white; text-decoration: none; border-radius: 8px; margin: 16px 0;">
            Скинути пароль
          </a>
          <p style="color: #999; font-size: 13px;">Посилання дійсне 1 годину. Якщо ви не запитували скидання — просто ігноруйте цей лист.</p>
        </div>
      `,
    });

    res.json({ message: "Якщо такий email існує, ми надішлемо інструкції" });
  } catch (error) {
    console.error("FORGOT PASSWORD ERROR:", error);
    res.status(500).json({ error: "Помилка відправки email" });
  }
});

// ==========================================
// RESET PASSWORD
// ==========================================

app.post("/api/auth/reset-password", async (req, res) => {
  const { token, password } = req.body;

  if (!token || !password) {
    return res.status(400).json({ error: "Токен та пароль обов'язкові" });
  }

  if (password.length < 6) {
    return res.status(400).json({ error: "Пароль мінімум 6 символів" });
  }

  try {
    const record = await prisma.passwordResetToken.findUnique({
      where: { token },
    });

    if (!record) {
      return res.status(400).json({ error: "Невалідний токен" });
    }

    if (record.expiresAt < new Date()) {
      await prisma.passwordResetToken.delete({ where: { token } });
      return res.status(400).json({ error: "Токен протермінований" });
    }

    const hashedPassword = await bcrypt.hash(password, 10);
    await prisma.user.update({
      where: { id: record.userId },
      data: { password: hashedPassword },
    });

    await prisma.passwordResetToken.delete({ where: { token } });

    res.json({ message: "Пароль успішно змінено" });
  } catch (error) {
    console.error("RESET PASSWORD ERROR:", error);
    res.status(500).json({ error: "Помилка зміни паролю" });
  }
});

// ==========================================
// ПРОФІЛЬ
// ==========================================

const getProfile = async (req, res) => {
  try {
    const user = await prisma.user.findUnique({
      where: { id: req.user.userId },
    });
    if (!user)
      return res.status(404).json({ error: "Користувача не знайдено" });
    res.json(formatUser(user));
  } catch (error) {
    console.error("GET PROFILE ERROR:", error);
    res.status(500).json({ error: "Помилка сервера" });
  }
};

app.get("/api/profile", authMiddleware, getProfile);
app.get("/api/auth/me", authMiddleware, getProfile);

// Парсить дату у форматі дд.мм.рррр (як вводить користувач на фронтенді)
// у коректний JS Date. Використання new Date(рядок) тут неприпустиме —
// Node.js читає крапки як американський формат MM.DD.YYYY.
function parseUkrainianDate(value) {
  if (!value) return null;
  const match = /^(\d{1,2})\.(\d{1,2})\.(\d{4})$/.exec(value.trim());
  if (!match) return null;
  const [, day, month, year] = match;
  const date = new Date(Date.UTC(Number(year), Number(month) - 1, Number(day)));
  return Number.isNaN(date.getTime()) ? null : date;
}

app.put("/api/profile", authMiddleware, async (req, res) => {
  try {
    const {
      firstName,
      lastName,
      middleName,
      phone,
      birthDate,
      gender,
      address,
    } = req.body;
    const updatedUser = await prisma.user.update({
      where: { id: req.user.userId },
      data: {
        firstName,
        lastName,
        middleName,
        phone,
        gender,
        address,
        birthDate: birthDate ? parseUkrainianDate(birthDate) : null,
      },
    });
    res.json(formatUser(updatedUser));
  } catch (error) {
    console.error("UPDATE PROFILE ERROR:", error);
    res.status(500).json({ error: "Помилка оновлення профілю" });
  }
});

// ==========================================
// КАТЕГОРІЇ — окремий роут (список + підкатегорії)
// ==========================================

app.get("/api/categories", async (req, res) => {
  try {
    const categories = await prisma.product.findMany({
      select: { category: true },
      distinct: ["category"],
      orderBy: { category: "asc" },
    });
    res.json(categories.map((c) => c.category));
  } catch (error) {
    console.error("GET CATEGORIES ERROR:", error);
    res.status(500).json({ error: "Помилка отримання категорій" });
  }
});

app.get("/api/categories/:category/subcategories", async (req, res) => {
  try {
    const { category } = req.params;
    const subcategories = await prisma.product.findMany({
      where: { category, subcategory: { not: null } },
      select: { subcategory: true },
      distinct: ["subcategory"],
      orderBy: { subcategory: "asc" },
    });
    res.json(subcategories.map((s) => s.subcategory));
  } catch (error) {
    console.error("GET SUBCATEGORIES ERROR:", error);
    res.status(500).json({ error: "Помилка отримання підкатегорій" });
  }
});

// ==========================================
// ТОВАРИ — публічні (GET) з пагінацією та сортуванням
// ==========================================

app.get("/api/products", async (req, res) => {
  try {
    const {
      category,
      subcategory,
      search,
      isPromo,
      page = 1,
      limit = 20,
      sortBy = "createdAt",
      order = "desc",
    } = req.query;

    const where = {};
    if (category) where.category = { contains: category, mode: "insensitive" };
    if (subcategory) where.subcategory = subcategory;
    if (search) where.name = { contains: search, mode: "insensitive" };
    if (isPromo === "true") where.isPromo = true;

    const skip = (Number(page) - 1) * Number(limit);

    const validSortFields = ["createdAt", "price", "rating", "name"];
    const validOrders = ["asc", "desc"];

    const orderBy = {
      [validSortFields.includes(sortBy) ? sortBy : "createdAt"]:
        validOrders.includes(order) ? order : "desc",
    };

    const [products, total] = await Promise.all([
      prisma.product.findMany({
        where,
        orderBy,
        skip,
        take: Number(limit),
        include: { images: true },
      }),
      prisma.product.count({ where }),
    ]);

    res.json({
      products: products.map(formatProduct),
      total,
      page: Number(page),
      limit: Number(limit),
      totalPages: Math.ceil(total / Number(limit)),
    });
  } catch (error) {
    console.error("GET PRODUCTS ERROR:", error);
    res.status(500).json({ error: "Помилка отримання товарів" });
  }
});

app.get("/api/products/:id", async (req, res) => {
  try {
    const product = await prisma.product.findUnique({
      where: { id: Number(req.params.id) },
      include: {
        images: true,
        reviews: {
          include: { user: { select: { firstName: true, lastName: true } } },
          orderBy: { createdAt: "desc" },
        },
      },
    });
    if (!product) return res.status(404).json({ error: "Товар не знайдено" });
    res.json(formatProductWithReviews(product));
  } catch (error) {
    res.status(500).json({ error: "Помилка отримання товару" });
  }
});

// ==========================================
// ТОВАРИ — тільки адмін (POST/PUT/DELETE)
// ==========================================

app.post("/api/products", authMiddleware, adminMiddleware, async (req, res) => {
  try {
    const {
      name,
      description,
      price,
      category,
      subcategory,
      image,
      images,
      oldPrice,
      isPromo,
    } = req.body;
    if (!name || !price)
      return res.status(400).json({ error: "Назва та ціна обов'язкові" });

    // image — перше фото з масиву, для сумісності зі старою схемою
    const firstImage = image || images?.[0] || null;

    const newProduct = await prisma.product.create({
      data: {
        name,
        description,
        price: Number(price),
        category,
        subcategory,
        image: firstImage,
        oldPrice: oldPrice ? Number(oldPrice) : undefined,
        isPromo: isPromo === true || isPromo === "true",
        rating: 0,
        images: {
          create: (images || []).map((url, index) => ({ url, order: index })),
        },
      },
      include: { images: true },
    });
    res.status(201).json(formatProduct(newProduct));
  } catch (error) {
    console.error("CREATE PRODUCT ERROR:", error);
    res.status(500).json({ error: "Помилка створення товару" });
  }
});

app.put(
  "/api/products/:id",
  authMiddleware,
  adminMiddleware,
  async (req, res) => {
    try {
      const {
        name,
        description,
        price,
        category,
        subcategory,
        image,
        images,
        oldPrice,
        isPromo,
      } = req.body;

      const firstImage = image || images?.[0] || null;

      const updateData = {
        name,
        description,
        price: price ? Number(price) : undefined,
        category,
        subcategory,
        image: firstImage,
        oldPrice: oldPrice !== undefined ? Number(oldPrice) : undefined,
        isPromo:
          isPromo !== undefined
            ? isPromo === true || isPromo === "true"
            : undefined,
      };

      // Оновлюємо фото тільки якщо images переданий у запиті —
      // інакше старі фото лишаються без змін (часткове оновлення товару)
      if (images !== undefined) {
        updateData.images = {
          deleteMany: {},
          create: images.map((url, index) => ({ url, order: index })),
        };
      }

      const updatedProduct = await prisma.product.update({
        where: { id: Number(req.params.id) },
        data: updateData,
        include: { images: true },
      });
      res.json(formatProduct(updatedProduct));
    } catch (error) {
      console.error("UPDATE PRODUCT ERROR:", error);
      res.status(500).json({ error: "Помилка оновлення товару" });
    }
  },
);

app.delete(
  "/api/products/:id",
  authMiddleware,
  adminMiddleware,
  async (req, res) => {
    try {
      await prisma.product.delete({ where: { id: Number(req.params.id) } });
      res.status(204).send();
    } catch (error) {
      console.error("DELETE PRODUCT ERROR:", error);
      res.status(500).json({ error: "Помилка видалення товару" });
    }
  },
);

// ==========================================
// РЕЦЕПТИ
// ==========================================

app.get("/api/recipes", async (req, res) => {
  try {
    const recipes = await prisma.recipe.findMany({
      orderBy: { createdAt: "desc" },
    });
    res.json(recipes.map(formatRecipe));
  } catch (error) {
    console.error("GET RECIPES ERROR:", error);
    res.status(500).json({ error: "Помилка отримання рецептів" });
  }
});

// ==========================================
// ВІДГУКИ
// ==========================================

app.get("/api/products/:id/reviews", async (req, res) => {
  try {
    const reviews = await prisma.review.findMany({
      where: { productId: Number(req.params.id) },
      include: { user: { select: { firstName: true, lastName: true } } },
      orderBy: { createdAt: "desc" },
    });
    res.json(reviews.map(formatReview));
  } catch (error) {
    res.status(500).json({ error: "Помилка отримання відгуків" });
  }
});

app.post("/api/products/:id/reviews", authMiddleware, async (req, res) => {
  try {
    const { rating, comment } = req.body;
    const productId = Number(req.params.id);

    if (!rating || rating < 1 || rating > 5) {
      return res.status(400).json({ error: "Рейтинг має бути від 1 до 5" });
    }

    const existing = await prisma.review.findFirst({
      where: { userId: req.user.userId, productId },
    });
    if (existing)
      return res
        .status(409)
        .json({ error: "Ви вже залишили відгук для цього товару" });

    const review = await prisma.review.create({
      data: {
        rating: Number(rating),
        comment,
        userId: req.user.userId,
        productId,
      },
      include: { user: { select: { firstName: true, lastName: true } } },
    });

    const allReviews = await prisma.review.findMany({ where: { productId } });
    const avgRating =
      allReviews.reduce((sum, r) => sum + r.rating, 0) / allReviews.length;
    await prisma.product.update({
      where: { id: productId },
      data: { rating: Math.round(avgRating * 10) / 10 },
    });

    res.status(201).json(formatReview(review));
  } catch (error) {
    console.error("CREATE REVIEW ERROR:", error);
    res.status(500).json({ error: "Помилка створення відгуку" });
  }
});

app.delete("/api/reviews/:id", authMiddleware, async (req, res) => {
  try {
    const review = await prisma.review.findUnique({
      where: { id: Number(req.params.id) },
    });
    if (!review) return res.status(404).json({ error: "Відгук не знайдено" });

    if (review.userId !== req.user.userId && !req.user.isAdmin) {
      return res
        .status(403)
        .json({ error: "Немає прав для видалення цього відгуку" });
    }

    await prisma.review.delete({ where: { id: Number(req.params.id) } });
    res.status(204).send();
  } catch (error) {
    res.status(500).json({ error: "Помилка видалення відгуку" });
  }
});

// ==========================================
// ЗАМОВЛЕННЯ
// ==========================================

app.post("/api/orders", authMiddleware, async (req, res) => {
  try {
    const { items, address, paymentMethod, comment } = req.body;

    if (!items || items.length === 0) {
      return res.status(400).json({ error: "Кошик порожній" });
    }

    // Безпека: ціну й назву товару беремо ТІЛЬКИ з бази даних,
    // а не з тіла запиту — інакше клієнт міг би підмінити ціну перед відправкою.
    const productIds = items.map((item) => Number(item.productId || item.id));
    const products = await prisma.product.findMany({
      where: { id: { in: productIds } },
    });
    const productsById = new Map(products.map((p) => [p.id, p]));

    const orderItemsData = [];
    for (const item of items) {
      const productId = Number(item.productId || item.id);
      const product = productsById.get(productId);
      if (!product) {
        return res
          .status(400)
          .json({ error: `Товар з id ${productId} не знайдено` });
      }
      const quantity = Number(item.quantity) || 1;
      orderItemsData.push({
        productId,
        name: product.name,
        price: product.price,
        quantity,
      });
    }

    const total = orderItemsData.reduce(
      (sum, item) => sum + item.price * item.quantity,
      0,
    );

    const order = await prisma.order.create({
      data: {
        userId: req.user.userId,
        total,
        address,
        paymentMethod: paymentMethod || "cash",
        comment,
        items: { create: orderItemsData },
      },
      include: { items: true },
    });

    res.status(201).json(formatOrder(order));
  } catch (error) {
    console.error("CREATE ORDER ERROR:", error);
    res.status(500).json({ error: "Помилка створення замовлення" });
  }
});

app.get("/api/orders/my", authMiddleware, async (req, res) => {
  try {
    const orders = await prisma.order.findMany({
      where: { userId: req.user.userId },
      include: { items: true },
      orderBy: { createdAt: "desc" },
    });
    res.json(orders.map(formatOrder));
  } catch (error) {
    console.error("GET MY ORDERS ERROR:", error);
    res.status(500).json({ error: "Помилка отримання замовлень" });
  }
});

app.get("/api/orders/:id", authMiddleware, async (req, res) => {
  try {
    const order = await prisma.order.findUnique({
      where: { id: Number(req.params.id) },
      include: { items: true },
    });

    if (!order)
      return res.status(404).json({ error: "Замовлення не знайдено" });

    if (order.userId !== req.user.userId && !req.user.isAdmin) {
      return res.status(403).json({ error: "Немає доступу" });
    }

    res.json(formatOrder(order));
  } catch (error) {
    res.status(500).json({ error: "Помилка отримання замовлення" });
  }
});

app.get("/api/orders", authMiddleware, adminMiddleware, async (req, res) => {
  try {
    const orders = await prisma.order.findMany({
      include: {
        items: true,
        user: { select: { email: true, firstName: true, lastName: true } },
      },
      orderBy: { createdAt: "desc" },
    });
    res.json(orders.map(formatOrder));
  } catch (error) {
    res.status(500).json({ error: "Помилка отримання замовлень" });
  }
});

app.patch(
  "/api/orders/:id/status",
  authMiddleware,
  adminMiddleware,
  async (req, res) => {
    try {
      const { status } = req.body;
      const validStatuses = [
        "pending",
        "confirmed",
        "preparing",
        "delivering",
        "delivered",
        "cancelled",
      ];

      if (!validStatuses.includes(status)) {
        return res.status(400).json({ error: "Невірний статус замовлення" });
      }

      const order = await prisma.order.update({
        where: { id: Number(req.params.id) },
        data: { status },
      });

      res.json(formatOrder(order));
    } catch (error) {
      res.status(500).json({ error: "Помилка оновлення статусу" });
    }
  },
);

// ==========================================
// АДМІН — список юзерів
// ==========================================

app.get(
  "/api/admin/users",
  authMiddleware,
  adminMiddleware,
  async (req, res) => {
    try {
      const users = await prisma.user.findMany({
        select: {
          id: true,
          email: true,
          firstName: true,
          lastName: true,
          isAdmin: true,
          createdAt: true,
        },
        orderBy: { createdAt: "desc" },
      });
      res.json(users.map(formatUserBrief));
    } catch (error) {
      res.status(500).json({ error: "Помилка отримання юзерів" });
    }
  },
);

// ==========================================
// ЗАВАНТАЖЕННЯ ЗОБРАЖЕНЬ (Cloudinary)
// ==========================================

cloudinary.config({
  cloud_name: process.env.CLOUDINARY_CLOUD_NAME,
  api_key: process.env.CLOUDINARY_API_KEY,
  api_secret: process.env.CLOUDINARY_API_SECRET,
});

const storage = multer.memoryStorage();

const upload = multer({
  storage,
  limits: { fileSize: 5 * 1024 * 1024 }, // 5MB
  fileFilter: (req, file, cb) => {
    const allowed = ["image/jpeg", "image/png", "image/webp"];
    if (allowed.includes(file.mimetype)) {
      cb(null, true);
    } else {
      cb(new Error("Тільки JPG, PNG, WEBP"));
    }
  },
});

app.post(
  "/api/upload",
  authMiddleware,
  adminMiddleware,
  upload.single("image"),
  async (req, res) => {
    try {
      if (!req.file)
        return res.status(400).json({ error: "Файл не завантажено" });

      const result = await new Promise((resolve, reject) => {
        cloudinary.uploader
          .upload_stream(
            { folder: "kalpo-shop", resource_type: "image" },
            (error, result) => {
              if (error) reject(error);
              else resolve(result);
            },
          )
          .end(req.file.buffer);
      });

      res
        .status(201)
        .json({ url: result.secure_url, publicId: result.public_id });
    } catch (error) {
      console.error("UPLOAD ERROR:", error);
      res.status(500).json({ error: "Помилка завантаження зображення" });
    }
  },
);

// ==========================================
// STRIPE ПЛАТЕЖІ
// ==========================================

app.post(
  "/api/payments/create-checkout-session",
  authMiddleware,
  async (req, res) => {
    try {
      const { orderId, items } = req.body;

      if (!items || items.length === 0) {
        return res.status(400).json({ error: "Кошик порожній" });
      }

      // Безпека: ціну й назву товару для Stripe беремо ТІЛЬКИ з бази даних,
      // інакше клієнт міг би підмінити ціну в запиті й оплатити копійки.
      const productIds = items.map((item) => Number(item.productId || item.id));
      const products = await prisma.product.findMany({
        where: { id: { in: productIds } },
      });
      const productsById = new Map(products.map((p) => [p.id, p]));

      const lineItems = [];
      for (const item of items) {
        const productId = Number(item.productId || item.id);
        const product = productsById.get(productId);
        if (!product) {
          return res
            .status(400)
            .json({ error: `Товар з id ${productId} не знайдено` });
        }
        lineItems.push({
          price_data: {
            currency: "uah",
            product_data: { name: product.name },
            unit_amount: Math.round(product.price * 100), // копійки
          },
          quantity: Number(item.quantity) || 1,
        });
      }

      const session = await stripe.checkout.sessions.create({
        payment_method_types: ["card"],
        mode: "payment",
        line_items: lineItems,
        success_url: `${process.env.FRONTEND_URL}/order-success?session_id={CHECKOUT_SESSION_ID}`,
        cancel_url: `${process.env.FRONTEND_URL}/checkout`,
        metadata: { orderId: orderId ? String(orderId) : "" },
      });

      res.status(201).json({ url: session.url });
    } catch (error) {
      console.error("STRIPE CREATE SESSION ERROR:", error);
      res.status(500).json({ error: "Не вдалося створити сесію оплати" });
    }
  },
);

app.get("/api/payments/verify-session", async (req, res) => {
  try {
    const { session_id } = req.query;
    if (!session_id) {
      return res.status(400).json({ error: "session_id відсутній" });
    }

    const session = await stripe.checkout.sessions.retrieve(session_id);
    const paid = session.payment_status === "paid";

    if (paid && session.metadata?.orderId) {
      await prisma.order.update({
        where: { id: Number(session.metadata.orderId) },
        data: { status: "paid" },
      });
    }

    res.json({ paid, orderId: session.metadata?.orderId || null });
  } catch (error) {
    console.error("STRIPE VERIFY SESSION ERROR:", error);
    res.status(500).json({ error: "Не вдалося перевірити оплату" });
  }
});

// ==========================================
// СТАРТ
// ==========================================

const PORT = process.env.PORT || 3000;
app.listen(PORT, () => console.log(`✅ Server running on port ${PORT}`));
</file>

<file path="test-db.js">
const { PrismaClient } = require("@prisma/client");

const prisma = new PrismaClient();

async function main() {
  const products = await prisma.product.findMany();
  console.log(products);
}

main()
  .catch(console.error)
  .finally(() => prisma.$disconnect());
</file>

</files>
